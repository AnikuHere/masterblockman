/* This file is generated by tdr. */
/* No manual modification is permitted. */

/* metalib version: 1 */
/* metalib md5sum: 65d0301c457c17200214e2e089e79aee */

/* creation time: Fri Apr 28 11:11:00 2023 */
/* tdr version: 2.5.0, build at 20111124 */

#ifndef _CSBattleProtocol_h_
#define _CSBattleProtocol_h_

#include "TdrBuf.h"
#include "TdrError.h"
#include "TdrBufUtil.h"
#include "TdrTypeUtil.h"

#include "CSBattleComm.h"

using tsf4g_tdr::TdrError;
using tsf4g_tdr::TdrReadBuf;
using tsf4g_tdr::TdrWriteBuf;

namespace BattleProtocol
{


#pragma pack(1)


class CSPoint // 玩家坐标
{
    public: /* members */
        int32_t x; // X轴
        int32_t y; // Y轴
        int32_t z; // Z轴

    public: /* methods */
        TdrError::ErrorType construct();

        TdrError::ErrorType pack(TdrWriteBuf& destBuf, unsigned cutVer = 0) const;
        TdrError::ErrorType pack(char* buffer, size_t size, size_t* usedSize = NULL, unsigned cutVer = 0) const;

        TdrError::ErrorType unpack(TdrReadBuf& srcBuf, unsigned cutVer = 0);
        TdrError::ErrorType unpack(const char* buffer, size_t size, size_t* usedSize = NULL, unsigned cutVer = 0);

        /* set indent = -1 to disable indent */
        TdrError::ErrorType visualize(TdrWriteBuf& destBuf, int indent = 0, char separator = '\n') const;
        TdrError::ErrorType visualize(char* buffer, size_t size, size_t* usedSize = NULL,
                                      int indent = 0, char separator = '\n') const;
        const char* visualize_ex(TdrWriteBuf& destBuf, int indent = 0, char separator = '\n') const;
        const char* visualize_ex(char* buffer, size_t size, size_t* usedSize = NULL,
                                 int indent = 0, char separator = '\n') const;


    public: /* version infomation */
        enum
        {
            BASEVERSION = 1,
            CURRVERSION = 1,
        };
};

typedef       CSPoint          tagCSPoint;
typedef       CSPoint             CSPOINT;
typedef       CSPoint*          LPCSPOINT;
typedef const CSPoint*         LCPCSPOINT;


class BattleSceneParams // 场景创建定义
{
    public: /* members */
        int32_t sceneID; // 场景ID

    public: /* methods */
        TdrError::ErrorType construct();

        TdrError::ErrorType pack(TdrWriteBuf& destBuf, unsigned cutVer = 0) const;
        TdrError::ErrorType pack(char* buffer, size_t size, size_t* usedSize = NULL, unsigned cutVer = 0) const;

        TdrError::ErrorType unpack(TdrReadBuf& srcBuf, unsigned cutVer = 0);
        TdrError::ErrorType unpack(const char* buffer, size_t size, size_t* usedSize = NULL, unsigned cutVer = 0);

        /* set indent = -1 to disable indent */
        TdrError::ErrorType visualize(TdrWriteBuf& destBuf, int indent = 0, char separator = '\n') const;
        TdrError::ErrorType visualize(char* buffer, size_t size, size_t* usedSize = NULL,
                                      int indent = 0, char separator = '\n') const;
        const char* visualize_ex(TdrWriteBuf& destBuf, int indent = 0, char separator = '\n') const;
        const char* visualize_ex(char* buffer, size_t size, size_t* usedSize = NULL,
                                 int indent = 0, char separator = '\n') const;


    public: /* version infomation */
        enum
        {
            BASEVERSION = 1,
            CURRVERSION = 1,
        };
};

typedef       BattleSceneParams          tagBattleSceneParams;
typedef       BattleSceneParams             BATTLESCENEPARAMS;
typedef       BattleSceneParams*          LPBATTLESCENEPARAMS;
typedef const BattleSceneParams*         LCPBATTLESCENEPARAMS;


class BattleHeroCreateParam // 英雄创建定义
{
    public: /* members */
        int32_t ResID; // 资源ID
        int32_t SkinID; // 皮肤ID

    public: /* methods */
        TdrError::ErrorType construct();

        TdrError::ErrorType pack(TdrWriteBuf& destBuf, unsigned cutVer = 0) const;
        TdrError::ErrorType pack(char* buffer, size_t size, size_t* usedSize = NULL, unsigned cutVer = 0) const;

        TdrError::ErrorType unpack(TdrReadBuf& srcBuf, unsigned cutVer = 0);
        TdrError::ErrorType unpack(const char* buffer, size_t size, size_t* usedSize = NULL, unsigned cutVer = 0);

        /* set indent = -1 to disable indent */
        TdrError::ErrorType visualize(TdrWriteBuf& destBuf, int indent = 0, char separator = '\n') const;
        TdrError::ErrorType visualize(char* buffer, size_t size, size_t* usedSize = NULL,
                                      int indent = 0, char separator = '\n') const;
        const char* visualize_ex(TdrWriteBuf& destBuf, int indent = 0, char separator = '\n') const;
        const char* visualize_ex(char* buffer, size_t size, size_t* usedSize = NULL,
                                 int indent = 0, char separator = '\n') const;


    public: /* version infomation */
        enum
        {
            BASEVERSION = 1,
            CURRVERSION = 1,
        };
};

typedef       BattleHeroCreateParam          tagBattleHeroCreateParam;
typedef       BattleHeroCreateParam             BATTLEHEROCREATEPARAM;
typedef       BattleHeroCreateParam*          LPBATTLEHEROCREATEPARAM;
typedef const BattleHeroCreateParam*         LCPBATTLEHEROCREATEPARAM;


class BattleTeamCreateSystemParam // 队伍系统设置创建定义
{
    public: /* members */
        int8_t Key; // 系统设置项
        int32_t Param1; // 系统设置参数1

    public: /* methods */
        TdrError::ErrorType construct();

        TdrError::ErrorType pack(TdrWriteBuf& destBuf, unsigned cutVer = 0) const;
        TdrError::ErrorType pack(char* buffer, size_t size, size_t* usedSize = NULL, unsigned cutVer = 0) const;

        TdrError::ErrorType unpack(TdrReadBuf& srcBuf, unsigned cutVer = 0);
        TdrError::ErrorType unpack(const char* buffer, size_t size, size_t* usedSize = NULL, unsigned cutVer = 0);

        /* set indent = -1 to disable indent */
        TdrError::ErrorType visualize(TdrWriteBuf& destBuf, int indent = 0, char separator = '\n') const;
        TdrError::ErrorType visualize(char* buffer, size_t size, size_t* usedSize = NULL,
                                      int indent = 0, char separator = '\n') const;
        const char* visualize_ex(TdrWriteBuf& destBuf, int indent = 0, char separator = '\n') const;
        const char* visualize_ex(char* buffer, size_t size, size_t* usedSize = NULL,
                                 int indent = 0, char separator = '\n') const;


    public: /* version infomation */
        enum
        {
            BASEVERSION = 1,
            CURRVERSION = 1,
        };
};

typedef       BattleTeamCreateSystemParam          tagBattleTeamCreateSystemParam;
typedef       BattleTeamCreateSystemParam             BATTLETEAMCREATESYSTEMPARAM;
typedef       BattleTeamCreateSystemParam*          LPBATTLETEAMCREATESYSTEMPARAM;
typedef const BattleTeamCreateSystemParam*         LCPBATTLETEAMCREATESYSTEMPARAM;


class BattleCreateRuneAttr // 符文属性
{
    public: /* members */
        int8_t AttrID; // 符文属性类型
        int8_t AttrAddType; // 符文属性加成类型
        int32_t Value; // 符文值

    public: /* methods */
        TdrError::ErrorType construct();

        TdrError::ErrorType pack(TdrWriteBuf& destBuf, unsigned cutVer = 0) const;
        TdrError::ErrorType pack(char* buffer, size_t size, size_t* usedSize = NULL, unsigned cutVer = 0) const;

        TdrError::ErrorType unpack(TdrReadBuf& srcBuf, unsigned cutVer = 0);
        TdrError::ErrorType unpack(const char* buffer, size_t size, size_t* usedSize = NULL, unsigned cutVer = 0);

        /* set indent = -1 to disable indent */
        TdrError::ErrorType visualize(TdrWriteBuf& destBuf, int indent = 0, char separator = '\n') const;
        TdrError::ErrorType visualize(char* buffer, size_t size, size_t* usedSize = NULL,
                                      int indent = 0, char separator = '\n') const;
        const char* visualize_ex(TdrWriteBuf& destBuf, int indent = 0, char separator = '\n') const;
        const char* visualize_ex(char* buffer, size_t size, size_t* usedSize = NULL,
                                 int indent = 0, char separator = '\n') const;


    public: /* version infomation */
        enum
        {
            BASEVERSION = 1,
            CURRVERSION = 1,
        };
};

typedef       BattleCreateRuneAttr          tagBattleCreateRuneAttr;
typedef       BattleCreateRuneAttr             BATTLECREATERUNEATTR;
typedef       BattleCreateRuneAttr*          LPBATTLECREATERUNEATTR;
typedef const BattleCreateRuneAttr*         LCPBATTLECREATERUNEATTR;


class BattleTeamCreateDataParam // 队伍战斗数值创建定义
{
    public: /* members */
        int8_t AiModeType; // AI类型
        int8_t bOpenAssist; // 是否开启AI托管
        int32_t TalentSkill; // 天赋技能
        int32_t SpySkill; // 侦查技能
        int8_t TechTreeType; // 科技树类型
        int32_t ExtDataID; // 附加数值ID,默认0(无)
        BattleCreateRuneAttr HeroRuneAttrs[MAX_ACTORRUNEATTR_COUNT]; // 英雄符文数值
        BattleCreateRuneAttr DogfaceRuneAttrs[MAX_ACTORRUNEATTR_COUNT]; // 士兵符文数值
        BattleCreateRuneAttr TeamRuneAttrs[MAX_TEAMRUNEATTR_COUNT]; // 科技符文数值

    public: /* methods */
        TdrError::ErrorType construct();

        TdrError::ErrorType pack(TdrWriteBuf& destBuf, unsigned cutVer = 0) const;
        TdrError::ErrorType pack(char* buffer, size_t size, size_t* usedSize = NULL, unsigned cutVer = 0) const;

        TdrError::ErrorType unpack(TdrReadBuf& srcBuf, unsigned cutVer = 0);
        TdrError::ErrorType unpack(const char* buffer, size_t size, size_t* usedSize = NULL, unsigned cutVer = 0);

        /* set indent = -1 to disable indent */
        TdrError::ErrorType visualize(TdrWriteBuf& destBuf, int indent = 0, char separator = '\n') const;
        TdrError::ErrorType visualize(char* buffer, size_t size, size_t* usedSize = NULL,
                                      int indent = 0, char separator = '\n') const;
        const char* visualize_ex(TdrWriteBuf& destBuf, int indent = 0, char separator = '\n') const;
        const char* visualize_ex(char* buffer, size_t size, size_t* usedSize = NULL,
                                 int indent = 0, char separator = '\n') const;


    public: /* version infomation */
        enum
        {
            BASEVERSION = 1,
            CURRVERSION = 1,
        };
};

typedef       BattleTeamCreateDataParam          tagBattleTeamCreateDataParam;
typedef       BattleTeamCreateDataParam             BATTLETEAMCREATEDATAPARAM;
typedef       BattleTeamCreateDataParam*          LPBATTLETEAMCREATEDATAPARAM;
typedef const BattleTeamCreateDataParam*         LCPBATTLETEAMCREATEDATAPARAM;


class BattleTeamCreateParams // 队伍创建信息
{
    public: /* members */
        int8_t bActive; // 是否激活
        int8_t legionType; // 阵营
        int8_t teamType; // 队伍类型
        int8_t raceType; // 种族
        BattleHeroCreateParam heroParams; // 英雄信息
        int8_t bIsPlayer; // 是否玩家
        int32_t PlayerID; // 玩家ID
        char PlayName[MAX_NAME_LEN]; // 玩家名字
        int32_t HeadIcon; // 玩家头像ID
        int8_t RankLevel; // 段位等级
        int8_t bControlPlayer; // 是否当前控制的
        CSPoint curPos; // 当前位置
        CSPoint curDir; // 当前朝向
        BattleTeamCreateDataParam Datas; // 数值创建定义
        BattleTeamCreateSystemParam Systems[MAX_BATTLESYSTEM_COUNT]; // 系统设置

    public: /* methods */
        TdrError::ErrorType construct();

        TdrError::ErrorType pack(TdrWriteBuf& destBuf, unsigned cutVer = 0) const;
        TdrError::ErrorType pack(char* buffer, size_t size, size_t* usedSize = NULL, unsigned cutVer = 0) const;

        TdrError::ErrorType unpack(TdrReadBuf& srcBuf, unsigned cutVer = 0);
        TdrError::ErrorType unpack(const char* buffer, size_t size, size_t* usedSize = NULL, unsigned cutVer = 0);

        /* set indent = -1 to disable indent */
        TdrError::ErrorType visualize(TdrWriteBuf& destBuf, int indent = 0, char separator = '\n') const;
        TdrError::ErrorType visualize(char* buffer, size_t size, size_t* usedSize = NULL,
                                      int indent = 0, char separator = '\n') const;
        const char* visualize_ex(TdrWriteBuf& destBuf, int indent = 0, char separator = '\n') const;
        const char* visualize_ex(char* buffer, size_t size, size_t* usedSize = NULL,
                                 int indent = 0, char separator = '\n') const;


    public: /* version infomation */
        enum
        {
            BASEVERSION = 1,
            CURRVERSION = 1,
        };
};

typedef       BattleTeamCreateParams          tagBattleTeamCreateParams;
typedef       BattleTeamCreateParams             BATTLETEAMCREATEPARAMS;
typedef       BattleTeamCreateParams*          LPBATTLETEAMCREATEPARAMS;
typedef const BattleTeamCreateParams*         LCPBATTLETEAMCREATEPARAMS;


class BattleCreateParams // 战场创建信息
{
    public: /* members */
        char GameVersion[MAX_NAME_LEN]; // 游戏版本号
        uint64_t battleID; // 战场ID
        int32_t randSeed; // 随机种子
        int32_t framePerSecond; // 帧率比
        BattleSceneParams sceneParams; // 场景
        BattleTeamCreateParams teamStateParams[MAX_BATTLTEAM_MAX_COUNT]; // 队伍

    public: /* methods */
        TdrError::ErrorType construct();

        TdrError::ErrorType pack(TdrWriteBuf& destBuf, unsigned cutVer = 0) const;
        TdrError::ErrorType pack(char* buffer, size_t size, size_t* usedSize = NULL, unsigned cutVer = 0) const;

        TdrError::ErrorType unpack(TdrReadBuf& srcBuf, unsigned cutVer = 0);
        TdrError::ErrorType unpack(const char* buffer, size_t size, size_t* usedSize = NULL, unsigned cutVer = 0);

        /* set indent = -1 to disable indent */
        TdrError::ErrorType visualize(TdrWriteBuf& destBuf, int indent = 0, char separator = '\n') const;
        TdrError::ErrorType visualize(char* buffer, size_t size, size_t* usedSize = NULL,
                                      int indent = 0, char separator = '\n') const;
        const char* visualize_ex(TdrWriteBuf& destBuf, int indent = 0, char separator = '\n') const;
        const char* visualize_ex(char* buffer, size_t size, size_t* usedSize = NULL,
                                 int indent = 0, char separator = '\n') const;


    public: /* version infomation */
        enum
        {
            BASEVERSION = 1,
            CURRVERSION = 1,
        };
};

typedef       BattleCreateParams          tagBattleCreateParams;
typedef       BattleCreateParams             BATTLECREATEPARAMS;
typedef       BattleCreateParams*          LPBATTLECREATEPARAMS;
typedef const BattleCreateParams*         LCPBATTLECREATEPARAMS;


class BattleReplaysTeamExtraInfo // 录像队伍额外信息
{
    public: /* members */
        int32_t totalGold; // 玩家金币总量
        int16_t heroKillCnt; // 英雄击杀数量
        int16_t dogfaceKillCnt; // 小兵击杀数量
        int8_t isMvp; // 是否MVP

    public: /* methods */
        TdrError::ErrorType construct();

        TdrError::ErrorType pack(TdrWriteBuf& destBuf, unsigned cutVer = 0) const;
        TdrError::ErrorType pack(char* buffer, size_t size, size_t* usedSize = NULL, unsigned cutVer = 0) const;

        TdrError::ErrorType unpack(TdrReadBuf& srcBuf, unsigned cutVer = 0);
        TdrError::ErrorType unpack(const char* buffer, size_t size, size_t* usedSize = NULL, unsigned cutVer = 0);

        /* set indent = -1 to disable indent */
        TdrError::ErrorType visualize(TdrWriteBuf& destBuf, int indent = 0, char separator = '\n') const;
        TdrError::ErrorType visualize(char* buffer, size_t size, size_t* usedSize = NULL,
                                      int indent = 0, char separator = '\n') const;
        const char* visualize_ex(TdrWriteBuf& destBuf, int indent = 0, char separator = '\n') const;
        const char* visualize_ex(char* buffer, size_t size, size_t* usedSize = NULL,
                                 int indent = 0, char separator = '\n') const;


    public: /* version infomation */
        enum
        {
            BASEVERSION = 1,
            CURRVERSION = 1,
        };
};

typedef       BattleReplaysTeamExtraInfo          tagBattleReplaysTeamExtraInfo;
typedef       BattleReplaysTeamExtraInfo             BATTLEREPLAYSTEAMEXTRAINFO;
typedef       BattleReplaysTeamExtraInfo*          LPBATTLEREPLAYSTEAMEXTRAINFO;
typedef const BattleReplaysTeamExtraInfo*         LCPBATTLEREPLAYSTEAMEXTRAINFO;


class BattleReplaysHeader // 录像头信息
{
    public: /* members */
        int32_t magic; // magic
        int32_t version; // 录像版本
        char gameVersion[MAX_NAME_LEN]; // 游戏版本号
        int8_t battleVsMode; // 战场对战模式
        uint64_t battleID; // 战场ID
        int32_t sceneID; // 场景ID
        int64_t createTime; // 录像创建时间戳
        int32_t frameCnt; // 帧数目,为0表示战斗中断
        int8_t battleResult; // 战场结果
        BattleCreateParams battleCreateParams; // 战场创建参数
        BattleReplaysTeamExtraInfo teamExtraInfos[MAX_BATTLTEAM_MAX_COUNT]; // 录像队伍信息列表

    public: /* methods */
        TdrError::ErrorType construct();

        TdrError::ErrorType pack(TdrWriteBuf& destBuf, unsigned cutVer = 0) const;
        TdrError::ErrorType pack(char* buffer, size_t size, size_t* usedSize = NULL, unsigned cutVer = 0) const;

        TdrError::ErrorType unpack(TdrReadBuf& srcBuf, unsigned cutVer = 0);
        TdrError::ErrorType unpack(const char* buffer, size_t size, size_t* usedSize = NULL, unsigned cutVer = 0);

        /* set indent = -1 to disable indent */
        TdrError::ErrorType visualize(TdrWriteBuf& destBuf, int indent = 0, char separator = '\n') const;
        TdrError::ErrorType visualize(char* buffer, size_t size, size_t* usedSize = NULL,
                                      int indent = 0, char separator = '\n') const;
        const char* visualize_ex(TdrWriteBuf& destBuf, int indent = 0, char separator = '\n') const;
        const char* visualize_ex(char* buffer, size_t size, size_t* usedSize = NULL,
                                 int indent = 0, char separator = '\n') const;


    public: /* version infomation */
        enum
        {
            BASEVERSION = 1,
            CURRVERSION = 1,
        };
};

typedef       BattleReplaysHeader          tagBattleReplaysHeader;
typedef       BattleReplaysHeader             BATTLEREPLAYSHEADER;
typedef       BattleReplaysHeader*          LPBATTLEREPLAYSHEADER;
typedef const BattleReplaysHeader*         LCPBATTLEREPLAYSHEADER;


class CSStatisticsActorSkillBar // 技能栏统计
{
    public: /* members */
        int32_t Dps; // 伤害
        int16_t CastCnt; // 释放次数
        int32_t ReHp; // 治疗量

    public: /* methods */
        TdrError::ErrorType construct();

        TdrError::ErrorType pack(TdrWriteBuf& destBuf, unsigned cutVer = 0) const;
        TdrError::ErrorType pack(char* buffer, size_t size, size_t* usedSize = NULL, unsigned cutVer = 0) const;

        TdrError::ErrorType unpack(TdrReadBuf& srcBuf, unsigned cutVer = 0);
        TdrError::ErrorType unpack(const char* buffer, size_t size, size_t* usedSize = NULL, unsigned cutVer = 0);

        /* set indent = -1 to disable indent */
        TdrError::ErrorType visualize(TdrWriteBuf& destBuf, int indent = 0, char separator = '\n') const;
        TdrError::ErrorType visualize(char* buffer, size_t size, size_t* usedSize = NULL,
                                      int indent = 0, char separator = '\n') const;
        const char* visualize_ex(TdrWriteBuf& destBuf, int indent = 0, char separator = '\n') const;
        const char* visualize_ex(char* buffer, size_t size, size_t* usedSize = NULL,
                                 int indent = 0, char separator = '\n') const;


    public: /* version infomation */
        enum
        {
            BASEVERSION = 1,
            CURRVERSION = 1,
        };
};

typedef       CSStatisticsActorSkillBar          tagCSStatisticsActorSkillBar;
typedef       CSStatisticsActorSkillBar             CSSTATISTICSACTORSKILLBAR;
typedef       CSStatisticsActorSkillBar*          LPCSSTATISTICSACTORSKILLBAR;
typedef const CSStatisticsActorSkillBar*         LCPCSSTATISTICSACTORSKILLBAR;


class CSStatisticsActor // actor统计信息
{
    public: /* members */
        int8_t actorType; // 单位类型
        int32_t ResID; // 资源ID
        int32_t SkinID; // 皮肤ID,只有英雄有
        int8_t Cnt; // 总个数
        int8_t LiveCnt; // 存活个数
        int32_t FristConstructTime; // 首次训练时间
        int16_t ConstuctGold; // 训练金币,计算价值量用
        int32_t TotalDps; // 总伤害
        int32_t TotalToHeroDps; // 对英雄伤害总量
        int32_t TotalToDogfaceDps; // 对小兵伤害总量
        int32_t TotalToBuildDps; // 对建筑伤害总量
        int32_t TotalToMonsterDps; // 对野怪伤害总量
        int32_t TotalNormalSkillDps; // 普通攻击总伤害
        int32_t TotalSkillsDps; // 技能总伤害
        CSStatisticsActorSkillBar SkillBars[MAX_BATTLSTATISSTIC_SKILLBARDMG_MAX_COUNT]; // 技能栏统计,QWER
        int32_t TotalReHp; // 治疗总量
        int32_t TotalDmgToler; // 承受伤害总量,排除野怪
        int32_t DogfaceDmgToler; // 承受小兵伤害总量
        int32_t HeroDmgToler; // 承受英雄伤害量
        int8_t BookAttrBuffLayer; // 属性书BUFF层数
        int32_t InBattleTime; // 进入战斗时间
        int16_t TeamBeKillCnt[MAX_BATTLTEAM_MAX_COUNT]; // 被队伍击杀统计

    public: /* methods */
        TdrError::ErrorType construct();

        TdrError::ErrorType pack(TdrWriteBuf& destBuf, unsigned cutVer = 0) const;
        TdrError::ErrorType pack(char* buffer, size_t size, size_t* usedSize = NULL, unsigned cutVer = 0) const;

        TdrError::ErrorType unpack(TdrReadBuf& srcBuf, unsigned cutVer = 0);
        TdrError::ErrorType unpack(const char* buffer, size_t size, size_t* usedSize = NULL, unsigned cutVer = 0);

        /* set indent = -1 to disable indent */
        TdrError::ErrorType visualize(TdrWriteBuf& destBuf, int indent = 0, char separator = '\n') const;
        TdrError::ErrorType visualize(char* buffer, size_t size, size_t* usedSize = NULL,
                                      int indent = 0, char separator = '\n') const;
        const char* visualize_ex(TdrWriteBuf& destBuf, int indent = 0, char separator = '\n') const;
        const char* visualize_ex(char* buffer, size_t size, size_t* usedSize = NULL,
                                 int indent = 0, char separator = '\n') const;


    public: /* version infomation */
        enum
        {
            BASEVERSION = 1,
            CURRVERSION = 1,
        };
};

typedef       CSStatisticsActor          tagCSStatisticsActor;
typedef       CSStatisticsActor             CSSTATISTICSACTOR;
typedef       CSStatisticsActor*          LPCSSTATISTICSACTOR;
typedef const CSStatisticsActor*         LCPCSSTATISTICSACTOR;


class CSStatisticsGoldMine // 分矿统计信息
{
    public: /* members */
        int8_t index; // 索引
        int32_t BuildTime; // 开矿时间(秒)
        int32_t DeadTime; // 矿死亡时间(秒)

    public: /* methods */
        TdrError::ErrorType construct();

        TdrError::ErrorType pack(TdrWriteBuf& destBuf, unsigned cutVer = 0) const;
        TdrError::ErrorType pack(char* buffer, size_t size, size_t* usedSize = NULL, unsigned cutVer = 0) const;

        TdrError::ErrorType unpack(TdrReadBuf& srcBuf, unsigned cutVer = 0);
        TdrError::ErrorType unpack(const char* buffer, size_t size, size_t* usedSize = NULL, unsigned cutVer = 0);

        /* set indent = -1 to disable indent */
        TdrError::ErrorType visualize(TdrWriteBuf& destBuf, int indent = 0, char separator = '\n') const;
        TdrError::ErrorType visualize(char* buffer, size_t size, size_t* usedSize = NULL,
                                      int indent = 0, char separator = '\n') const;
        const char* visualize_ex(TdrWriteBuf& destBuf, int indent = 0, char separator = '\n') const;
        const char* visualize_ex(char* buffer, size_t size, size_t* usedSize = NULL,
                                 int indent = 0, char separator = '\n') const;


    public: /* version infomation */
        enum
        {
            BASEVERSION = 1,
            CURRVERSION = 1,
        };
};

typedef       CSStatisticsGoldMine          tagCSStatisticsGoldMine;
typedef       CSStatisticsGoldMine             CSSTATISTICSGOLDMINE;
typedef       CSStatisticsGoldMine*          LPCSSTATISTICSGOLDMINE;
typedef const CSStatisticsGoldMine*         LCPCSSTATISTICSGOLDMINE;


class CSStatisticsTechNologys // 队伍科技统计信息
{
    public: /* members */
        int8_t TechLv; // 科技等级
        int16_t UpgradeTime2; // 升级2本时间(秒)
        int16_t UpgradeTime3; // 升级3本时间(秒)
        CSStatisticsGoldMine GoldMines[MAX_BATTLSTATISSTIC_GOLDMINE_MAX_COUNT]; // 分矿统计
        int16_t FoodUpTech1; // 升级1级人口时间(秒)
        int16_t FoodUpTech2; // 升级2级人口时间(秒)
        int16_t FoodUpTech3; // 升级3级人口时间(秒)
        int16_t FoodUpTech4; // 升级4级人口时间(秒)
        int16_t AtkUpTech1; // 升到1级攻击时间(秒)
        int16_t AtkUpTech2; // 升到2级攻击时间(秒)
        int16_t AtkUpTech3; // 升到3级攻击时间(秒)
        int16_t ArmorUpTech1; // 升到1级防御的时间(秒)
        int16_t ArmorUpTech2; // 升到2级防御的时间(秒)
        int16_t ArmorUpTech3; // 升到3级防御的时间(秒)

    public: /* methods */
        TdrError::ErrorType construct();

        TdrError::ErrorType pack(TdrWriteBuf& destBuf, unsigned cutVer = 0) const;
        TdrError::ErrorType pack(char* buffer, size_t size, size_t* usedSize = NULL, unsigned cutVer = 0) const;

        TdrError::ErrorType unpack(TdrReadBuf& srcBuf, unsigned cutVer = 0);
        TdrError::ErrorType unpack(const char* buffer, size_t size, size_t* usedSize = NULL, unsigned cutVer = 0);

        /* set indent = -1 to disable indent */
        TdrError::ErrorType visualize(TdrWriteBuf& destBuf, int indent = 0, char separator = '\n') const;
        TdrError::ErrorType visualize(char* buffer, size_t size, size_t* usedSize = NULL,
                                      int indent = 0, char separator = '\n') const;
        const char* visualize_ex(TdrWriteBuf& destBuf, int indent = 0, char separator = '\n') const;
        const char* visualize_ex(char* buffer, size_t size, size_t* usedSize = NULL,
                                 int indent = 0, char separator = '\n') const;


    public: /* version infomation */
        enum
        {
            BASEVERSION = 1,
            CURRVERSION = 1,
        };
};

typedef       CSStatisticsTechNologys          tagCSStatisticsTechNologys;
typedef       CSStatisticsTechNologys             CSSTATISTICSTECHNOLOGYS;
typedef       CSStatisticsTechNologys*          LPCSSTATISTICSTECHNOLOGYS;
typedef const CSStatisticsTechNologys*         LCPCSSTATISTICSTECHNOLOGYS;


class CSStatisticsDmg // 队伍伤害统计信息
{
    public: /* members */
        int16_t KillHeroCnt; // 击杀英雄个数
        int16_t KillMonsterCnt; // 击杀野怪个数
        int16_t KillDogfaceCnt; // 击杀士兵个数
        int8_t HeroBeKillCnt; // 英雄死亡次数
        int8_t BuildGodmine; // 造矿个数
        int8_t BuildTowerCnt; // 造塔个数
        int8_t killBigDragonCnt; // 杀大龙
        int8_t killSmallDragonCnt; // 杀小龙
        int8_t killBigTiTanCnt; // 杀大泰坦
        int8_t killSmallTiTanCnt; // 杀小泰坦
        int8_t KillTowerCnt; // 摧毁防御塔个数
        int8_t KillGoldMineCnt; // 摧毁金矿个数
        int8_t KillSubBaseCnt; // 摧毁副基地个数
        int8_t KillBaseCnt; // 摧毁基地个数
        int32_t KillPrice; // 击杀单位总价值
        int32_t AlivePrice; // 存活单位总价值
        int8_t KillHeroLegendaryCnt; // 连续击杀N个英雄次数(王者再临)
        int8_t KillDogfaceGodLikeCnt; // 连续击杀N个小兵次数(超神)

    public: /* methods */
        TdrError::ErrorType construct();

        TdrError::ErrorType pack(TdrWriteBuf& destBuf, unsigned cutVer = 0) const;
        TdrError::ErrorType pack(char* buffer, size_t size, size_t* usedSize = NULL, unsigned cutVer = 0) const;

        TdrError::ErrorType unpack(TdrReadBuf& srcBuf, unsigned cutVer = 0);
        TdrError::ErrorType unpack(const char* buffer, size_t size, size_t* usedSize = NULL, unsigned cutVer = 0);

        /* set indent = -1 to disable indent */
        TdrError::ErrorType visualize(TdrWriteBuf& destBuf, int indent = 0, char separator = '\n') const;
        TdrError::ErrorType visualize(char* buffer, size_t size, size_t* usedSize = NULL,
                                      int indent = 0, char separator = '\n') const;
        const char* visualize_ex(TdrWriteBuf& destBuf, int indent = 0, char separator = '\n') const;
        const char* visualize_ex(char* buffer, size_t size, size_t* usedSize = NULL,
                                 int indent = 0, char separator = '\n') const;


    public: /* version infomation */
        enum
        {
            BASEVERSION = 1,
            CURRVERSION = 1,
        };
};

typedef       CSStatisticsDmg          tagCSStatisticsDmg;
typedef       CSStatisticsDmg             CSSTATISTICSDMG;
typedef       CSStatisticsDmg*          LPCSSTATISTICSDMG;
typedef const CSStatisticsDmg*         LCPCSSTATISTICSDMG;


class CSStatisticsGold // 队伍金币统计信息
{
    public: /* members */
        int32_t TotalGold; // 累计金币
        int32_t SystemGold; // 系统获取
        int32_t GoldMineGold; // 金矿获取
        int16_t KillMonsterGold; // 击杀野怪获取
        int16_t KillActorGold; // 击杀单位获取
        int16_t RepariGold; // 维修消耗
        int16_t BuildDogfaceGold; // 造兵消耗
        int16_t TechUpGold; // 科技升级消耗
        int16_t BuildOtherGold; // 造塔造矿消耗
        int32_t GoldTimeline[MAX_BATTLSTATISSTIC_GOLDGET_MAX_COUNT]; // 金币获取统计时间,2000一个档

    public: /* methods */
        TdrError::ErrorType construct();

        TdrError::ErrorType pack(TdrWriteBuf& destBuf, unsigned cutVer = 0) const;
        TdrError::ErrorType pack(char* buffer, size_t size, size_t* usedSize = NULL, unsigned cutVer = 0) const;

        TdrError::ErrorType unpack(TdrReadBuf& srcBuf, unsigned cutVer = 0);
        TdrError::ErrorType unpack(const char* buffer, size_t size, size_t* usedSize = NULL, unsigned cutVer = 0);

        /* set indent = -1 to disable indent */
        TdrError::ErrorType visualize(TdrWriteBuf& destBuf, int indent = 0, char separator = '\n') const;
        TdrError::ErrorType visualize(char* buffer, size_t size, size_t* usedSize = NULL,
                                      int indent = 0, char separator = '\n') const;
        const char* visualize_ex(TdrWriteBuf& destBuf, int indent = 0, char separator = '\n') const;
        const char* visualize_ex(char* buffer, size_t size, size_t* usedSize = NULL,
                                 int indent = 0, char separator = '\n') const;


    public: /* version infomation */
        enum
        {
            BASEVERSION = 1,
            CURRVERSION = 1,
        };
};

typedef       CSStatisticsGold          tagCSStatisticsGold;
typedef       CSStatisticsGold             CSSTATISTICSGOLD;
typedef       CSStatisticsGold*          LPCSSTATISTICSGOLD;
typedef const CSStatisticsGold*         LCPCSSTATISTICSGOLD;


class CSStatisticsEquip // 队伍英雄装备统计信息
{
    public: /* members */
        int32_t EquipID[MAX_BATTLSTATISSTIC_EQUIP_MAX_COUNT]; // 装备ID列表

    public: /* methods */
        TdrError::ErrorType construct();

        TdrError::ErrorType pack(TdrWriteBuf& destBuf, unsigned cutVer = 0) const;
        TdrError::ErrorType pack(char* buffer, size_t size, size_t* usedSize = NULL, unsigned cutVer = 0) const;

        TdrError::ErrorType unpack(TdrReadBuf& srcBuf, unsigned cutVer = 0);
        TdrError::ErrorType unpack(const char* buffer, size_t size, size_t* usedSize = NULL, unsigned cutVer = 0);

        /* set indent = -1 to disable indent */
        TdrError::ErrorType visualize(TdrWriteBuf& destBuf, int indent = 0, char separator = '\n') const;
        TdrError::ErrorType visualize(char* buffer, size_t size, size_t* usedSize = NULL,
                                      int indent = 0, char separator = '\n') const;
        const char* visualize_ex(TdrWriteBuf& destBuf, int indent = 0, char separator = '\n') const;
        const char* visualize_ex(char* buffer, size_t size, size_t* usedSize = NULL,
                                 int indent = 0, char separator = '\n') const;


    public: /* version infomation */
        enum
        {
            BASEVERSION = 1,
            CURRVERSION = 1,
        };
};

typedef       CSStatisticsEquip          tagCSStatisticsEquip;
typedef       CSStatisticsEquip             CSSTATISTICSEQUIP;
typedef       CSStatisticsEquip*          LPCSSTATISTICSEQUIP;
typedef const CSStatisticsEquip*         LCPCSSTATISTICSEQUIP;


class CSStatisticsTeam // 队伍统计信息
{
    public: /* members */
        int8_t Active; // 是否激活
        int32_t PlayID; // 玩家ID
        char PlayName[MAX_NAME_LEN]; // 玩家名字
        int8_t TeamType; // 队伍类型 
        int8_t RaceType; // 种族类型
        int8_t LegionType; // 阵营类型
        int8_t HeroLv; // 英雄等级
        int8_t ActorResIDCnt; // ActorResID类型个数
        int32_t BattleScore; // MVP分数
        int8_t IsMvp; // 是否MVP
        int8_t RankLevel; // 段位等级
        CSStatisticsActor ActorStatistic[MAX_BATTLSTATISSTICD_TECHDOGFACE_MAX_COUNT]; // actor统计信息
        CSStatisticsTechNologys TechStatistic; // 队伍科技统计信息
        CSStatisticsDmg DmgStatistics; // 伤害统计信息
        CSStatisticsGold GoldStatistics; // 金币统计信息
        CSStatisticsEquip EquipStatistics; // 装备统计信息

    public: /* methods */
        TdrError::ErrorType construct();

        TdrError::ErrorType pack(TdrWriteBuf& destBuf, unsigned cutVer = 0) const;
        TdrError::ErrorType pack(char* buffer, size_t size, size_t* usedSize = NULL, unsigned cutVer = 0) const;

        TdrError::ErrorType unpack(TdrReadBuf& srcBuf, unsigned cutVer = 0);
        TdrError::ErrorType unpack(const char* buffer, size_t size, size_t* usedSize = NULL, unsigned cutVer = 0);

        /* set indent = -1 to disable indent */
        TdrError::ErrorType visualize(TdrWriteBuf& destBuf, int indent = 0, char separator = '\n') const;
        TdrError::ErrorType visualize(char* buffer, size_t size, size_t* usedSize = NULL,
                                      int indent = 0, char separator = '\n') const;
        const char* visualize_ex(TdrWriteBuf& destBuf, int indent = 0, char separator = '\n') const;
        const char* visualize_ex(char* buffer, size_t size, size_t* usedSize = NULL,
                                 int indent = 0, char separator = '\n') const;


    public: /* version infomation */
        enum
        {
            BASEVERSION = 1,
            CURRVERSION = 1,
        };
};

typedef       CSStatisticsTeam          tagCSStatisticsTeam;
typedef       CSStatisticsTeam             CSSTATISTICSTEAM;
typedef       CSStatisticsTeam*          LPCSSTATISTICSTEAM;
typedef const CSStatisticsTeam*         LCPCSSTATISTICSTEAM;


class CSStatisticsDropItem // 道具掉落统计
{
    public: /* members */
        int32_t ItemID; // 道具ID
        int8_t DropCnt; // 掉落次数

    public: /* methods */
        TdrError::ErrorType construct();

        TdrError::ErrorType pack(TdrWriteBuf& destBuf, unsigned cutVer = 0) const;
        TdrError::ErrorType pack(char* buffer, size_t size, size_t* usedSize = NULL, unsigned cutVer = 0) const;

        TdrError::ErrorType unpack(TdrReadBuf& srcBuf, unsigned cutVer = 0);
        TdrError::ErrorType unpack(const char* buffer, size_t size, size_t* usedSize = NULL, unsigned cutVer = 0);

        /* set indent = -1 to disable indent */
        TdrError::ErrorType visualize(TdrWriteBuf& destBuf, int indent = 0, char separator = '\n') const;
        TdrError::ErrorType visualize(char* buffer, size_t size, size_t* usedSize = NULL,
                                      int indent = 0, char separator = '\n') const;
        const char* visualize_ex(TdrWriteBuf& destBuf, int indent = 0, char separator = '\n') const;
        const char* visualize_ex(char* buffer, size_t size, size_t* usedSize = NULL,
                                 int indent = 0, char separator = '\n') const;


    public: /* version infomation */
        enum
        {
            BASEVERSION = 1,
            CURRVERSION = 1,
        };
};

typedef       CSStatisticsDropItem          tagCSStatisticsDropItem;
typedef       CSStatisticsDropItem             CSSTATISTICSDROPITEM;
typedef       CSStatisticsDropItem*          LPCSSTATISTICSDROPITEM;
typedef const CSStatisticsDropItem*         LCPCSSTATISTICSDROPITEM;


class CSStatisticsMonsterGroup // 怪物组统计信息
{
    public: /* members */
        int8_t JusticeKillCnt; // 正义方击杀次数
        int8_t EvalGroupKillCnt; // 邪恶方击杀次数

    public: /* methods */
        TdrError::ErrorType construct();

        TdrError::ErrorType pack(TdrWriteBuf& destBuf, unsigned cutVer = 0) const;
        TdrError::ErrorType pack(char* buffer, size_t size, size_t* usedSize = NULL, unsigned cutVer = 0) const;

        TdrError::ErrorType unpack(TdrReadBuf& srcBuf, unsigned cutVer = 0);
        TdrError::ErrorType unpack(const char* buffer, size_t size, size_t* usedSize = NULL, unsigned cutVer = 0);

        /* set indent = -1 to disable indent */
        TdrError::ErrorType visualize(TdrWriteBuf& destBuf, int indent = 0, char separator = '\n') const;
        TdrError::ErrorType visualize(char* buffer, size_t size, size_t* usedSize = NULL,
                                      int indent = 0, char separator = '\n') const;
        const char* visualize_ex(TdrWriteBuf& destBuf, int indent = 0, char separator = '\n') const;
        const char* visualize_ex(char* buffer, size_t size, size_t* usedSize = NULL,
                                 int indent = 0, char separator = '\n') const;


    public: /* version infomation */
        enum
        {
            BASEVERSION = 1,
            CURRVERSION = 1,
        };
};

typedef       CSStatisticsMonsterGroup          tagCSStatisticsMonsterGroup;
typedef       CSStatisticsMonsterGroup             CSSTATISTICSMONSTERGROUP;
typedef       CSStatisticsMonsterGroup*          LPCSSTATISTICSMONSTERGROUP;
typedef const CSStatisticsMonsterGroup*         LCPCSSTATISTICSMONSTERGROUP;


class CSStatisticsMap // 地图信息统计
{
    public: /* members */
        CSStatisticsMonsterGroup MonsterGroups[MAX_BATTLSTATISSTIC_MONSTER_GROUP_KILL_MAX_COUNT]; // 野怪组击统计
        CSStatisticsDropItem EquipDropItems[MAX_BATTLSTATISSTIC_DROP_ITEM_MAX_COUNT]; // 道具掉落统计

    public: /* methods */
        TdrError::ErrorType construct();

        TdrError::ErrorType pack(TdrWriteBuf& destBuf, unsigned cutVer = 0) const;
        TdrError::ErrorType pack(char* buffer, size_t size, size_t* usedSize = NULL, unsigned cutVer = 0) const;

        TdrError::ErrorType unpack(TdrReadBuf& srcBuf, unsigned cutVer = 0);
        TdrError::ErrorType unpack(const char* buffer, size_t size, size_t* usedSize = NULL, unsigned cutVer = 0);

        /* set indent = -1 to disable indent */
        TdrError::ErrorType visualize(TdrWriteBuf& destBuf, int indent = 0, char separator = '\n') const;
        TdrError::ErrorType visualize(char* buffer, size_t size, size_t* usedSize = NULL,
                                      int indent = 0, char separator = '\n') const;
        const char* visualize_ex(TdrWriteBuf& destBuf, int indent = 0, char separator = '\n') const;
        const char* visualize_ex(char* buffer, size_t size, size_t* usedSize = NULL,
                                 int indent = 0, char separator = '\n') const;


    public: /* version infomation */
        enum
        {
            BASEVERSION = 1,
            CURRVERSION = 1,
        };
};

typedef       CSStatisticsMap          tagCSStatisticsMap;
typedef       CSStatisticsMap             CSSTATISTICSMAP;
typedef       CSStatisticsMap*          LPCSSTATISTICSMAP;
typedef const CSStatisticsMap*         LCPCSSTATISTICSMAP;


class CSStatisticsBattle // 战场统计信息
{
    public: /* members */
        uint64_t BattleID; // 战场唯一ID
        int16_t BattleTime; // 战斗时长（秒） 
        int16_t BattleSceneID; // 战场地图ID
        int8_t BattleSceneType; // 战斗场景模式
        int8_t BattleResult; // 战斗结果
        int8_t WinerLegionType; // 赢的阵营
        int16_t Justice_score; // 正义阵营积分
        int16_t Evil_score; // 邪恶阵营积分
        CSStatisticsTeam TeamsStatistic[MAX_BATTLTEAM_MAX_COUNT]; // 队伍列表
        CSStatisticsMap MapStatistic; // 地图信息统计

    public: /* methods */
        TdrError::ErrorType construct();

        TdrError::ErrorType pack(TdrWriteBuf& destBuf, unsigned cutVer = 0) const;
        TdrError::ErrorType pack(char* buffer, size_t size, size_t* usedSize = NULL, unsigned cutVer = 0) const;

        TdrError::ErrorType unpack(TdrReadBuf& srcBuf, unsigned cutVer = 0);
        TdrError::ErrorType unpack(const char* buffer, size_t size, size_t* usedSize = NULL, unsigned cutVer = 0);

        /* set indent = -1 to disable indent */
        TdrError::ErrorType visualize(TdrWriteBuf& destBuf, int indent = 0, char separator = '\n') const;
        TdrError::ErrorType visualize(char* buffer, size_t size, size_t* usedSize = NULL,
                                      int indent = 0, char separator = '\n') const;
        const char* visualize_ex(TdrWriteBuf& destBuf, int indent = 0, char separator = '\n') const;
        const char* visualize_ex(char* buffer, size_t size, size_t* usedSize = NULL,
                                 int indent = 0, char separator = '\n') const;


    public: /* version infomation */
        enum
        {
            BASEVERSION = 1,
            CURRVERSION = 1,
        };
};

typedef       CSStatisticsBattle          tagCSStatisticsBattle;
typedef       CSStatisticsBattle             CSSTATISTICSBATTLE;
typedef       CSStatisticsBattle*          LPCSSTATISTICSBATTLE;
typedef const CSStatisticsBattle*         LCPCSSTATISTICSBATTLE;


class CSBattleLoginReq // 登陆请求
{
    public: /* members */
        char AccessToken[MAX_TOKEN_LEN]; // 角色访问token,测试用
        int32_t PlayerID; // 玩家ID
        int32_t RoomID; // 房间ID
        int8_t LoginType; // 重连标志 LoginConnectType
        char ProtocolMd5[MAX_MD5_LEN]; // 协议版本md5

    public: /* methods */
        TdrError::ErrorType construct();

        TdrError::ErrorType pack(TdrWriteBuf& destBuf, unsigned cutVer = 0) const;
        TdrError::ErrorType pack(char* buffer, size_t size, size_t* usedSize = NULL, unsigned cutVer = 0) const;

        TdrError::ErrorType unpack(TdrReadBuf& srcBuf, unsigned cutVer = 0);
        TdrError::ErrorType unpack(const char* buffer, size_t size, size_t* usedSize = NULL, unsigned cutVer = 0);

        /* set indent = -1 to disable indent */
        TdrError::ErrorType visualize(TdrWriteBuf& destBuf, int indent = 0, char separator = '\n') const;
        TdrError::ErrorType visualize(char* buffer, size_t size, size_t* usedSize = NULL,
                                      int indent = 0, char separator = '\n') const;
        const char* visualize_ex(TdrWriteBuf& destBuf, int indent = 0, char separator = '\n') const;
        const char* visualize_ex(char* buffer, size_t size, size_t* usedSize = NULL,
                                 int indent = 0, char separator = '\n') const;


    public: /* version infomation */
        enum
        {
            BASEVERSION = 1,
            CURRVERSION = 1,
        };
};

typedef       CSBattleLoginReq          tagCSBattleLoginReq;
typedef       CSBattleLoginReq             CSBATTLELOGINREQ;
typedef       CSBattleLoginReq*          LPCSBATTLELOGINREQ;
typedef const CSBattleLoginReq*         LCPCSBATTLELOGINREQ;


class CSBattleLoginRsp // 登陆请求返回
{
    public: /* members */
        int32_t RetCode; // 返回码
        int32_t PlayerID; // 玩家ID
        int8_t LoginType; // 重连标志 LoginConnectType
        BattleCreateParams CreateParams; // 战斗创建数据
        int32_t FrameSync; // 是否开启帧同步

    public: /* methods */
        TdrError::ErrorType construct();

        TdrError::ErrorType pack(TdrWriteBuf& destBuf, unsigned cutVer = 0) const;
        TdrError::ErrorType pack(char* buffer, size_t size, size_t* usedSize = NULL, unsigned cutVer = 0) const;

        TdrError::ErrorType unpack(TdrReadBuf& srcBuf, unsigned cutVer = 0);
        TdrError::ErrorType unpack(const char* buffer, size_t size, size_t* usedSize = NULL, unsigned cutVer = 0);

        /* set indent = -1 to disable indent */
        TdrError::ErrorType visualize(TdrWriteBuf& destBuf, int indent = 0, char separator = '\n') const;
        TdrError::ErrorType visualize(char* buffer, size_t size, size_t* usedSize = NULL,
                                      int indent = 0, char separator = '\n') const;
        const char* visualize_ex(TdrWriteBuf& destBuf, int indent = 0, char separator = '\n') const;
        const char* visualize_ex(char* buffer, size_t size, size_t* usedSize = NULL,
                                 int indent = 0, char separator = '\n') const;


    public: /* version infomation */
        enum
        {
            BASEVERSION = 1,
            CURRVERSION = 1,
        };
};

typedef       CSBattleLoginRsp          tagCSBattleLoginRsp;
typedef       CSBattleLoginRsp             CSBATTLELOGINRSP;
typedef       CSBattleLoginRsp*          LPCSBATTLELOGINRSP;
typedef const CSBattleLoginRsp*         LCPCSBATTLELOGINRSP;


class CSHeartBeatReq // 心跳包
{
    public: /* members */
        int32_t ClientTime; // 客户端时间

    public: /* methods */
        TdrError::ErrorType construct();

        TdrError::ErrorType pack(TdrWriteBuf& destBuf, unsigned cutVer = 0) const;
        TdrError::ErrorType pack(char* buffer, size_t size, size_t* usedSize = NULL, unsigned cutVer = 0) const;

        TdrError::ErrorType unpack(TdrReadBuf& srcBuf, unsigned cutVer = 0);
        TdrError::ErrorType unpack(const char* buffer, size_t size, size_t* usedSize = NULL, unsigned cutVer = 0);

        /* set indent = -1 to disable indent */
        TdrError::ErrorType visualize(TdrWriteBuf& destBuf, int indent = 0, char separator = '\n') const;
        TdrError::ErrorType visualize(char* buffer, size_t size, size_t* usedSize = NULL,
                                      int indent = 0, char separator = '\n') const;
        const char* visualize_ex(TdrWriteBuf& destBuf, int indent = 0, char separator = '\n') const;
        const char* visualize_ex(char* buffer, size_t size, size_t* usedSize = NULL,
                                 int indent = 0, char separator = '\n') const;


    public: /* version infomation */
        enum
        {
            BASEVERSION = 1,
            CURRVERSION = 1,
        };
};

typedef       CSHeartBeatReq          tagCSHeartBeatReq;
typedef       CSHeartBeatReq             CSHEARTBEATREQ;
typedef       CSHeartBeatReq*          LPCSHEARTBEATREQ;
typedef const CSHeartBeatReq*         LCPCSHEARTBEATREQ;


class CSHeartBeatRsp // 心跳回包
{
    public: /* members */
        int32_t ClientTime; // 客户端时间

    public: /* methods */
        TdrError::ErrorType construct();

        TdrError::ErrorType pack(TdrWriteBuf& destBuf, unsigned cutVer = 0) const;
        TdrError::ErrorType pack(char* buffer, size_t size, size_t* usedSize = NULL, unsigned cutVer = 0) const;

        TdrError::ErrorType unpack(TdrReadBuf& srcBuf, unsigned cutVer = 0);
        TdrError::ErrorType unpack(const char* buffer, size_t size, size_t* usedSize = NULL, unsigned cutVer = 0);

        /* set indent = -1 to disable indent */
        TdrError::ErrorType visualize(TdrWriteBuf& destBuf, int indent = 0, char separator = '\n') const;
        TdrError::ErrorType visualize(char* buffer, size_t size, size_t* usedSize = NULL,
                                      int indent = 0, char separator = '\n') const;
        const char* visualize_ex(TdrWriteBuf& destBuf, int indent = 0, char separator = '\n') const;
        const char* visualize_ex(char* buffer, size_t size, size_t* usedSize = NULL,
                                 int indent = 0, char separator = '\n') const;


    public: /* version infomation */
        enum
        {
            BASEVERSION = 1,
            CURRVERSION = 1,
        };
};

typedef       CSHeartBeatRsp          tagCSHeartBeatRsp;
typedef       CSHeartBeatRsp             CSHEARTBEATRSP;
typedef       CSHeartBeatRsp*          LPCSHEARTBEATRSP;
typedef const CSHeartBeatRsp*         LCPCSHEARTBEATRSP;


class CSMapLoadProgressReq
{
    public: /* members */
        int8_t Progress; // 进度

    public: /* methods */
        TdrError::ErrorType construct();

        TdrError::ErrorType pack(TdrWriteBuf& destBuf, unsigned cutVer = 0) const;
        TdrError::ErrorType pack(char* buffer, size_t size, size_t* usedSize = NULL, unsigned cutVer = 0) const;

        TdrError::ErrorType unpack(TdrReadBuf& srcBuf, unsigned cutVer = 0);
        TdrError::ErrorType unpack(const char* buffer, size_t size, size_t* usedSize = NULL, unsigned cutVer = 0);

        /* set indent = -1 to disable indent */
        TdrError::ErrorType visualize(TdrWriteBuf& destBuf, int indent = 0, char separator = '\n') const;
        TdrError::ErrorType visualize(char* buffer, size_t size, size_t* usedSize = NULL,
                                      int indent = 0, char separator = '\n') const;
        const char* visualize_ex(TdrWriteBuf& destBuf, int indent = 0, char separator = '\n') const;
        const char* visualize_ex(char* buffer, size_t size, size_t* usedSize = NULL,
                                 int indent = 0, char separator = '\n') const;


    public: /* version infomation */
        enum
        {
            BASEVERSION = 1,
            CURRVERSION = 1,
        };
};

typedef       CSMapLoadProgressReq          tagCSMapLoadProgressReq;
typedef       CSMapLoadProgressReq             CSMAPLOADPROGRESSREQ;
typedef       CSMapLoadProgressReq*          LPCSMAPLOADPROGRESSREQ;
typedef const CSMapLoadProgressReq*         LCPCSMAPLOADPROGRESSREQ;


class CSMapLoadProgressNotify
{
    public: /* members */
        int32_t PlayerID; // 玩家ID
        int8_t Progress; // 进度

    public: /* methods */
        TdrError::ErrorType construct();

        TdrError::ErrorType pack(TdrWriteBuf& destBuf, unsigned cutVer = 0) const;
        TdrError::ErrorType pack(char* buffer, size_t size, size_t* usedSize = NULL, unsigned cutVer = 0) const;

        TdrError::ErrorType unpack(TdrReadBuf& srcBuf, unsigned cutVer = 0);
        TdrError::ErrorType unpack(const char* buffer, size_t size, size_t* usedSize = NULL, unsigned cutVer = 0);

        /* set indent = -1 to disable indent */
        TdrError::ErrorType visualize(TdrWriteBuf& destBuf, int indent = 0, char separator = '\n') const;
        TdrError::ErrorType visualize(char* buffer, size_t size, size_t* usedSize = NULL,
                                      int indent = 0, char separator = '\n') const;
        const char* visualize_ex(TdrWriteBuf& destBuf, int indent = 0, char separator = '\n') const;
        const char* visualize_ex(char* buffer, size_t size, size_t* usedSize = NULL,
                                 int indent = 0, char separator = '\n') const;


    public: /* version infomation */
        enum
        {
            BASEVERSION = 1,
            CURRVERSION = 1,
        };
};

typedef       CSMapLoadProgressNotify          tagCSMapLoadProgressNotify;
typedef       CSMapLoadProgressNotify             CSMAPLOADPROGRESSNOTIFY;
typedef       CSMapLoadProgressNotify*          LPCSMAPLOADPROGRESSNOTIFY;
typedef const CSMapLoadProgressNotify*         LCPCSMAPLOADPROGRESSNOTIFY;


class CSFinishLoadMapReq // 加载地图
{
    public: /* members */
        uint8_t Reserved; // 保留字段

    public: /* methods */
        TdrError::ErrorType construct();

        TdrError::ErrorType pack(TdrWriteBuf& destBuf, unsigned cutVer = 0) const;
        TdrError::ErrorType pack(char* buffer, size_t size, size_t* usedSize = NULL, unsigned cutVer = 0) const;

        TdrError::ErrorType unpack(TdrReadBuf& srcBuf, unsigned cutVer = 0);
        TdrError::ErrorType unpack(const char* buffer, size_t size, size_t* usedSize = NULL, unsigned cutVer = 0);

        /* set indent = -1 to disable indent */
        TdrError::ErrorType visualize(TdrWriteBuf& destBuf, int indent = 0, char separator = '\n') const;
        TdrError::ErrorType visualize(char* buffer, size_t size, size_t* usedSize = NULL,
                                      int indent = 0, char separator = '\n') const;
        const char* visualize_ex(TdrWriteBuf& destBuf, int indent = 0, char separator = '\n') const;
        const char* visualize_ex(char* buffer, size_t size, size_t* usedSize = NULL,
                                 int indent = 0, char separator = '\n') const;


    public: /* version infomation */
        enum
        {
            BASEVERSION = 1,
            CURRVERSION = 1,
        };
};

typedef       CSFinishLoadMapReq          tagCSFinishLoadMapReq;
typedef       CSFinishLoadMapReq             CSFINISHLOADMAPREQ;
typedef       CSFinishLoadMapReq*          LPCSFINISHLOADMAPREQ;
typedef const CSFinishLoadMapReq*         LCPCSFINISHLOADMAPREQ;


class CSFinishLoadMapRsp // 完成加载地图
{
    public: /* members */
        uint8_t Reserved; // 保留字段

    public: /* methods */
        TdrError::ErrorType construct();

        TdrError::ErrorType pack(TdrWriteBuf& destBuf, unsigned cutVer = 0) const;
        TdrError::ErrorType pack(char* buffer, size_t size, size_t* usedSize = NULL, unsigned cutVer = 0) const;

        TdrError::ErrorType unpack(TdrReadBuf& srcBuf, unsigned cutVer = 0);
        TdrError::ErrorType unpack(const char* buffer, size_t size, size_t* usedSize = NULL, unsigned cutVer = 0);

        /* set indent = -1 to disable indent */
        TdrError::ErrorType visualize(TdrWriteBuf& destBuf, int indent = 0, char separator = '\n') const;
        TdrError::ErrorType visualize(char* buffer, size_t size, size_t* usedSize = NULL,
                                      int indent = 0, char separator = '\n') const;
        const char* visualize_ex(TdrWriteBuf& destBuf, int indent = 0, char separator = '\n') const;
        const char* visualize_ex(char* buffer, size_t size, size_t* usedSize = NULL,
                                 int indent = 0, char separator = '\n') const;


    public: /* version infomation */
        enum
        {
            BASEVERSION = 1,
            CURRVERSION = 1,
        };
};

typedef       CSFinishLoadMapRsp          tagCSFinishLoadMapRsp;
typedef       CSFinishLoadMapRsp             CSFINISHLOADMAPRSP;
typedef       CSFinishLoadMapRsp*          LPCSFINISHLOADMAPRSP;
typedef const CSFinishLoadMapRsp*         LCPCSFINISHLOADMAPRSP;


class CSGameStartNotify // 开始游戏通知
{
    public: /* members */
        uint64_t BattleID; // 战场唯一ID
        int32_t FrameSync; // 是否开启帧同步

    public: /* methods */
        TdrError::ErrorType construct();

        TdrError::ErrorType pack(TdrWriteBuf& destBuf, unsigned cutVer = 0) const;
        TdrError::ErrorType pack(char* buffer, size_t size, size_t* usedSize = NULL, unsigned cutVer = 0) const;

        TdrError::ErrorType unpack(TdrReadBuf& srcBuf, unsigned cutVer = 0);
        TdrError::ErrorType unpack(const char* buffer, size_t size, size_t* usedSize = NULL, unsigned cutVer = 0);

        /* set indent = -1 to disable indent */
        TdrError::ErrorType visualize(TdrWriteBuf& destBuf, int indent = 0, char separator = '\n') const;
        TdrError::ErrorType visualize(char* buffer, size_t size, size_t* usedSize = NULL,
                                      int indent = 0, char separator = '\n') const;
        const char* visualize_ex(TdrWriteBuf& destBuf, int indent = 0, char separator = '\n') const;
        const char* visualize_ex(char* buffer, size_t size, size_t* usedSize = NULL,
                                 int indent = 0, char separator = '\n') const;


    public: /* version infomation */
        enum
        {
            BASEVERSION = 1,
            CURRVERSION = 1,
        };
};

typedef       CSGameStartNotify          tagCSGameStartNotify;
typedef       CSGameStartNotify             CSGAMESTARTNOTIFY;
typedef       CSGameStartNotify*          LPCSGAMESTARTNOTIFY;
typedef const CSGameStartNotify*         LCPCSGAMESTARTNOTIFY;


class CSGameReStartReq // 重连开始游戏请求
{
    public: /* members */
        int32_t LastFrame; // 客户端最后处理帧号

    public: /* methods */
        TdrError::ErrorType construct();

        TdrError::ErrorType pack(TdrWriteBuf& destBuf, unsigned cutVer = 0) const;
        TdrError::ErrorType pack(char* buffer, size_t size, size_t* usedSize = NULL, unsigned cutVer = 0) const;

        TdrError::ErrorType unpack(TdrReadBuf& srcBuf, unsigned cutVer = 0);
        TdrError::ErrorType unpack(const char* buffer, size_t size, size_t* usedSize = NULL, unsigned cutVer = 0);

        /* set indent = -1 to disable indent */
        TdrError::ErrorType visualize(TdrWriteBuf& destBuf, int indent = 0, char separator = '\n') const;
        TdrError::ErrorType visualize(char* buffer, size_t size, size_t* usedSize = NULL,
                                      int indent = 0, char separator = '\n') const;
        const char* visualize_ex(TdrWriteBuf& destBuf, int indent = 0, char separator = '\n') const;
        const char* visualize_ex(char* buffer, size_t size, size_t* usedSize = NULL,
                                 int indent = 0, char separator = '\n') const;


    public: /* version infomation */
        enum
        {
            BASEVERSION = 1,
            CURRVERSION = 1,
        };
};

typedef       CSGameReStartReq          tagCSGameReStartReq;
typedef       CSGameReStartReq             CSGAMERESTARTREQ;
typedef       CSGameReStartReq*          LPCSGAMERESTARTREQ;
typedef const CSGameReStartReq*         LCPCSGAMERESTARTREQ;


class CSGameReStartRsp // 重连开始游戏回包
{
    public: /* members */
        int8_t State; // 服务器重连状态
        int32_t StartFrame; // 开始帧号
        int32_t ServersFrame; // 服务器最新帧号
        int32_t FrameCnt; // 帧数目
        int32_t FrameDataBuffSize; // buff大小
        int8_t FrameDataBuff[MAX_BATTLEFRAMECMDARRAY_MAXSIZE]; // 帧BUFF

    public: /* methods */
        TdrError::ErrorType construct();

        TdrError::ErrorType pack(TdrWriteBuf& destBuf, unsigned cutVer = 0) const;
        TdrError::ErrorType pack(char* buffer, size_t size, size_t* usedSize = NULL, unsigned cutVer = 0) const;

        TdrError::ErrorType unpack(TdrReadBuf& srcBuf, unsigned cutVer = 0);
        TdrError::ErrorType unpack(const char* buffer, size_t size, size_t* usedSize = NULL, unsigned cutVer = 0);

        /* set indent = -1 to disable indent */
        TdrError::ErrorType visualize(TdrWriteBuf& destBuf, int indent = 0, char separator = '\n') const;
        TdrError::ErrorType visualize(char* buffer, size_t size, size_t* usedSize = NULL,
                                      int indent = 0, char separator = '\n') const;
        const char* visualize_ex(TdrWriteBuf& destBuf, int indent = 0, char separator = '\n') const;
        const char* visualize_ex(char* buffer, size_t size, size_t* usedSize = NULL,
                                 int indent = 0, char separator = '\n') const;


    public: /* version infomation */
        enum
        {
            BASEVERSION = 1,
            CURRVERSION = 1,
        };
};

typedef       CSGameReStartRsp          tagCSGameReStartRsp;
typedef       CSGameReStartRsp             CSGAMERESTARTRSP;
typedef       CSGameReStartRsp*          LPCSGAMERESTARTRSP;
typedef const CSGameReStartRsp*         LCPCSGAMERESTARTRSP;


class CSGameEndReq // 结束比赛上报
{
    public: /* members */
        int32_t frame; // 帧号
        CSStatisticsBattle statisticsBattle; // 游戏统计数据
        int32_t dumpLength; // 长度
        char dumpInfo[MAX_DUMP_LEN]; // dump数据

    public: /* methods */
        TdrError::ErrorType construct();

        TdrError::ErrorType pack(TdrWriteBuf& destBuf, unsigned cutVer = 0) const;
        TdrError::ErrorType pack(char* buffer, size_t size, size_t* usedSize = NULL, unsigned cutVer = 0) const;

        TdrError::ErrorType unpack(TdrReadBuf& srcBuf, unsigned cutVer = 0);
        TdrError::ErrorType unpack(const char* buffer, size_t size, size_t* usedSize = NULL, unsigned cutVer = 0);

        /* set indent = -1 to disable indent */
        TdrError::ErrorType visualize(TdrWriteBuf& destBuf, int indent = 0, char separator = '\n') const;
        TdrError::ErrorType visualize(char* buffer, size_t size, size_t* usedSize = NULL,
                                      int indent = 0, char separator = '\n') const;
        const char* visualize_ex(TdrWriteBuf& destBuf, int indent = 0, char separator = '\n') const;
        const char* visualize_ex(char* buffer, size_t size, size_t* usedSize = NULL,
                                 int indent = 0, char separator = '\n') const;


    public: /* version infomation */
        enum
        {
            BASEVERSION = 1,
            CURRVERSION = 1,
        };
};

typedef       CSGameEndReq          tagCSGameEndReq;
typedef       CSGameEndReq             CSGAMEENDREQ;
typedef       CSGameEndReq*          LPCSGAMEENDREQ;
typedef const CSGameEndReq*         LCPCSGAMEENDREQ;


class CSGameEndNotify // 结束比赛
{
    public: /* members */
        uint8_t Winner; // 赢方阵营
        int32_t EndReason; // 结束原因

    public: /* methods */
        TdrError::ErrorType construct();

        TdrError::ErrorType pack(TdrWriteBuf& destBuf, unsigned cutVer = 0) const;
        TdrError::ErrorType pack(char* buffer, size_t size, size_t* usedSize = NULL, unsigned cutVer = 0) const;

        TdrError::ErrorType unpack(TdrReadBuf& srcBuf, unsigned cutVer = 0);
        TdrError::ErrorType unpack(const char* buffer, size_t size, size_t* usedSize = NULL, unsigned cutVer = 0);

        /* set indent = -1 to disable indent */
        TdrError::ErrorType visualize(TdrWriteBuf& destBuf, int indent = 0, char separator = '\n') const;
        TdrError::ErrorType visualize(char* buffer, size_t size, size_t* usedSize = NULL,
                                      int indent = 0, char separator = '\n') const;
        const char* visualize_ex(TdrWriteBuf& destBuf, int indent = 0, char separator = '\n') const;
        const char* visualize_ex(char* buffer, size_t size, size_t* usedSize = NULL,
                                 int indent = 0, char separator = '\n') const;


    public: /* version infomation */
        enum
        {
            BASEVERSION = 1,
            CURRVERSION = 1,
        };
};

typedef       CSGameEndNotify          tagCSGameEndNotify;
typedef       CSGameEndNotify             CSGAMEENDNOTIFY;
typedef       CSGameEndNotify*          LPCSGAMEENDNOTIFY;
typedef const CSGameEndNotify*         LCPCSGAMEENDNOTIFY;


class CSGmCommand // gm指令
{
    public: /* members */
        int32_t cmd_length; // 长度
        char gm_command[MAX_GM_LEN]; // gm指令

    public: /* methods */
        TdrError::ErrorType construct();

        TdrError::ErrorType pack(TdrWriteBuf& destBuf, unsigned cutVer = 0) const;
        TdrError::ErrorType pack(char* buffer, size_t size, size_t* usedSize = NULL, unsigned cutVer = 0) const;

        TdrError::ErrorType unpack(TdrReadBuf& srcBuf, unsigned cutVer = 0);
        TdrError::ErrorType unpack(const char* buffer, size_t size, size_t* usedSize = NULL, unsigned cutVer = 0);

        /* set indent = -1 to disable indent */
        TdrError::ErrorType visualize(TdrWriteBuf& destBuf, int indent = 0, char separator = '\n') const;
        TdrError::ErrorType visualize(char* buffer, size_t size, size_t* usedSize = NULL,
                                      int indent = 0, char separator = '\n') const;
        const char* visualize_ex(TdrWriteBuf& destBuf, int indent = 0, char separator = '\n') const;
        const char* visualize_ex(char* buffer, size_t size, size_t* usedSize = NULL,
                                 int indent = 0, char separator = '\n') const;


    public: /* version infomation */
        enum
        {
            BASEVERSION = 1,
            CURRVERSION = 1,
        };
};

typedef       CSGmCommand          tagCSGmCommand;
typedef       CSGmCommand             CSGMCOMMAND;
typedef       CSGmCommand*          LPCSGMCOMMAND;
typedef const CSGmCommand*         LCPCSGMCOMMAND;


class CSDumpInfo // dumpInfo
{
    public: /* members */
        int32_t frame; // 帧号
        int32_t ProtoLength; // 长度
        char ProtoMD5[MAX_DUMP_LEN]; // 协议数据MD5
        int32_t length; // 长度
        char dumpInfo[MAX_DUMP_LEN]; // dump数据

    public: /* methods */
        TdrError::ErrorType construct();

        TdrError::ErrorType pack(TdrWriteBuf& destBuf, unsigned cutVer = 0) const;
        TdrError::ErrorType pack(char* buffer, size_t size, size_t* usedSize = NULL, unsigned cutVer = 0) const;

        TdrError::ErrorType unpack(TdrReadBuf& srcBuf, unsigned cutVer = 0);
        TdrError::ErrorType unpack(const char* buffer, size_t size, size_t* usedSize = NULL, unsigned cutVer = 0);

        /* set indent = -1 to disable indent */
        TdrError::ErrorType visualize(TdrWriteBuf& destBuf, int indent = 0, char separator = '\n') const;
        TdrError::ErrorType visualize(char* buffer, size_t size, size_t* usedSize = NULL,
                                      int indent = 0, char separator = '\n') const;
        const char* visualize_ex(TdrWriteBuf& destBuf, int indent = 0, char separator = '\n') const;
        const char* visualize_ex(char* buffer, size_t size, size_t* usedSize = NULL,
                                 int indent = 0, char separator = '\n') const;


    public: /* version infomation */
        enum
        {
            BASEVERSION = 1,
            CURRVERSION = 1,
        };
};

typedef       CSDumpInfo          tagCSDumpInfo;
typedef       CSDumpInfo             CSDUMPINFO;
typedef       CSDumpInfo*          LPCSDUMPINFO;
typedef const CSDumpInfo*         LCPCSDUMPINFO;


class CSDumpInfoRet // dumpInfo
{
    public: /* members */
        int32_t frame; // 帧号

    public: /* methods */
        TdrError::ErrorType construct();

        TdrError::ErrorType pack(TdrWriteBuf& destBuf, unsigned cutVer = 0) const;
        TdrError::ErrorType pack(char* buffer, size_t size, size_t* usedSize = NULL, unsigned cutVer = 0) const;

        TdrError::ErrorType unpack(TdrReadBuf& srcBuf, unsigned cutVer = 0);
        TdrError::ErrorType unpack(const char* buffer, size_t size, size_t* usedSize = NULL, unsigned cutVer = 0);

        /* set indent = -1 to disable indent */
        TdrError::ErrorType visualize(TdrWriteBuf& destBuf, int indent = 0, char separator = '\n') const;
        TdrError::ErrorType visualize(char* buffer, size_t size, size_t* usedSize = NULL,
                                      int indent = 0, char separator = '\n') const;
        const char* visualize_ex(TdrWriteBuf& destBuf, int indent = 0, char separator = '\n') const;
        const char* visualize_ex(char* buffer, size_t size, size_t* usedSize = NULL,
                                 int indent = 0, char separator = '\n') const;


    public: /* version infomation */
        enum
        {
            BASEVERSION = 1,
            CURRVERSION = 1,
        };
};

typedef       CSDumpInfoRet          tagCSDumpInfoRet;
typedef       CSDumpInfoRet             CSDUMPINFORET;
typedef       CSDumpInfoRet*          LPCSDUMPINFORET;
typedef const CSDumpInfoRet*         LCPCSDUMPINFORET;


class CSDumpBattleActorBuff // dump战斗单位Buff
{
    public: /* members */
        int32_t BuffID; // 帧号
        int32_t ContinueTime; // 持续时间
        int32_t StepTime; // 当前进行的时间

    public: /* methods */
        TdrError::ErrorType construct();

        TdrError::ErrorType pack(TdrWriteBuf& destBuf, unsigned cutVer = 0) const;
        TdrError::ErrorType pack(char* buffer, size_t size, size_t* usedSize = NULL, unsigned cutVer = 0) const;

        TdrError::ErrorType unpack(TdrReadBuf& srcBuf, unsigned cutVer = 0);
        TdrError::ErrorType unpack(const char* buffer, size_t size, size_t* usedSize = NULL, unsigned cutVer = 0);

        /* set indent = -1 to disable indent */
        TdrError::ErrorType visualize(TdrWriteBuf& destBuf, int indent = 0, char separator = '\n') const;
        TdrError::ErrorType visualize(char* buffer, size_t size, size_t* usedSize = NULL,
                                      int indent = 0, char separator = '\n') const;
        const char* visualize_ex(TdrWriteBuf& destBuf, int indent = 0, char separator = '\n') const;
        const char* visualize_ex(char* buffer, size_t size, size_t* usedSize = NULL,
                                 int indent = 0, char separator = '\n') const;


    public: /* version infomation */
        enum
        {
            BASEVERSION = 1,
            CURRVERSION = 1,
        };
};

typedef       CSDumpBattleActorBuff          tagCSDumpBattleActorBuff;
typedef       CSDumpBattleActorBuff             CSDUMPBATTLEACTORBUFF;
typedef       CSDumpBattleActorBuff*          LPCSDUMPBATTLEACTORBUFF;
typedef const CSDumpBattleActorBuff*         LCPCSDUMPBATTLEACTORBUFF;


class CSDumpBattleActorInfo // dump战斗单位信息
{
    public: /* members */
        int32_t ObjID; // 帧号
        int32_t ResID; // 帧号
        CSPoint TargetPos; // 位置
        CSPoint TargetDir; // 朝向
        int8_t AttrCnt; // 属性个数
        int32_t AttrVal[MAX_BATTLTEDUMPATTR_MAX_COUNT]; // 属性信息
        int8_t BuffCnt; // Buff个数
        CSDumpBattleActorBuff BuffVal[MAX_BATTLTEBUFF_MAX_COUNT]; // BUFF信息

    public: /* methods */
        TdrError::ErrorType construct();

        TdrError::ErrorType pack(TdrWriteBuf& destBuf, unsigned cutVer = 0) const;
        TdrError::ErrorType pack(char* buffer, size_t size, size_t* usedSize = NULL, unsigned cutVer = 0) const;

        TdrError::ErrorType unpack(TdrReadBuf& srcBuf, unsigned cutVer = 0);
        TdrError::ErrorType unpack(const char* buffer, size_t size, size_t* usedSize = NULL, unsigned cutVer = 0);

        /* set indent = -1 to disable indent */
        TdrError::ErrorType visualize(TdrWriteBuf& destBuf, int indent = 0, char separator = '\n') const;
        TdrError::ErrorType visualize(char* buffer, size_t size, size_t* usedSize = NULL,
                                      int indent = 0, char separator = '\n') const;
        const char* visualize_ex(TdrWriteBuf& destBuf, int indent = 0, char separator = '\n') const;
        const char* visualize_ex(char* buffer, size_t size, size_t* usedSize = NULL,
                                 int indent = 0, char separator = '\n') const;


    public: /* version infomation */
        enum
        {
            BASEVERSION = 1,
            CURRVERSION = 1,
        };
};

typedef       CSDumpBattleActorInfo          tagCSDumpBattleActorInfo;
typedef       CSDumpBattleActorInfo             CSDUMPBATTLEACTORINFO;
typedef       CSDumpBattleActorInfo*          LPCSDUMPBATTLEACTORINFO;
typedef const CSDumpBattleActorInfo*         LCPCSDUMPBATTLEACTORINFO;


class CSDumpTeamInfo // dump队伍信息
{
    public: /* members */
        int8_t TeamType; // 队伍类型
        int8_t RaceType; // 种族类型
        int8_t LegionType; // 阵营类型
        int32_t Gold; // 金钱
        int8_t Food; // 人口
        int32_t ActorCnt; // 单位个数
        CSDumpBattleActorInfo ActorInfo[MAX_BATTLTACTOR_MAX_COUNT]; // 单位信息

    public: /* methods */
        TdrError::ErrorType construct();

        TdrError::ErrorType pack(TdrWriteBuf& destBuf, unsigned cutVer = 0) const;
        TdrError::ErrorType pack(char* buffer, size_t size, size_t* usedSize = NULL, unsigned cutVer = 0) const;

        TdrError::ErrorType unpack(TdrReadBuf& srcBuf, unsigned cutVer = 0);
        TdrError::ErrorType unpack(const char* buffer, size_t size, size_t* usedSize = NULL, unsigned cutVer = 0);

        /* set indent = -1 to disable indent */
        TdrError::ErrorType visualize(TdrWriteBuf& destBuf, int indent = 0, char separator = '\n') const;
        TdrError::ErrorType visualize(char* buffer, size_t size, size_t* usedSize = NULL,
                                      int indent = 0, char separator = '\n') const;
        const char* visualize_ex(TdrWriteBuf& destBuf, int indent = 0, char separator = '\n') const;
        const char* visualize_ex(char* buffer, size_t size, size_t* usedSize = NULL,
                                 int indent = 0, char separator = '\n') const;


    public: /* version infomation */
        enum
        {
            BASEVERSION = 1,
            CURRVERSION = 1,
        };
};

typedef       CSDumpTeamInfo          tagCSDumpTeamInfo;
typedef       CSDumpTeamInfo             CSDUMPTEAMINFO;
typedef       CSDumpTeamInfo*          LPCSDUMPTEAMINFO;
typedef const CSDumpTeamInfo*         LCPCSDUMPTEAMINFO;


class CSDumpBattleFrameInfo // dump 1帧战场信息
{
    public: /* members */
        int32_t FrameCnt; // 帧号
        int8_t TeamCnt; // 队伍个数
        CSDumpTeamInfo TeamInfo[MAX_BATTLTEAM_MAX_COUNT]; // 队伍信息

    public: /* methods */
        TdrError::ErrorType construct();

        TdrError::ErrorType pack(TdrWriteBuf& destBuf, unsigned cutVer = 0) const;
        TdrError::ErrorType pack(char* buffer, size_t size, size_t* usedSize = NULL, unsigned cutVer = 0) const;

        TdrError::ErrorType unpack(TdrReadBuf& srcBuf, unsigned cutVer = 0);
        TdrError::ErrorType unpack(const char* buffer, size_t size, size_t* usedSize = NULL, unsigned cutVer = 0);

        /* set indent = -1 to disable indent */
        TdrError::ErrorType visualize(TdrWriteBuf& destBuf, int indent = 0, char separator = '\n') const;
        TdrError::ErrorType visualize(char* buffer, size_t size, size_t* usedSize = NULL,
                                      int indent = 0, char separator = '\n') const;
        const char* visualize_ex(TdrWriteBuf& destBuf, int indent = 0, char separator = '\n') const;
        const char* visualize_ex(char* buffer, size_t size, size_t* usedSize = NULL,
                                 int indent = 0, char separator = '\n') const;


    public: /* version infomation */
        enum
        {
            BASEVERSION = 1,
            CURRVERSION = 1,
        };
};

typedef       CSDumpBattleFrameInfo          tagCSDumpBattleFrameInfo;
typedef       CSDumpBattleFrameInfo             CSDUMPBATTLEFRAMEINFO;
typedef       CSDumpBattleFrameInfo*          LPCSDUMPBATTLEFRAMEINFO;
typedef const CSDumpBattleFrameInfo*         LCPCSDUMPBATTLEFRAMEINFO;


class CSFrameActorCastSkill // 释放技能请求
{
    public: /* members */
        int32_t SkillID; // 技能ID
        CSPoint CaseDir; // 释放方向
        int32_t TargetObjID; // 目标ObjID
        CSPoint HitPos; // 点击目标坐标

    public: /* methods */
        TdrError::ErrorType construct();

        TdrError::ErrorType pack(TdrWriteBuf& destBuf, unsigned cutVer = 0) const;
        TdrError::ErrorType pack(char* buffer, size_t size, size_t* usedSize = NULL, unsigned cutVer = 0) const;

        TdrError::ErrorType unpack(TdrReadBuf& srcBuf, unsigned cutVer = 0);
        TdrError::ErrorType unpack(const char* buffer, size_t size, size_t* usedSize = NULL, unsigned cutVer = 0);

        /* set indent = -1 to disable indent */
        TdrError::ErrorType visualize(TdrWriteBuf& destBuf, int indent = 0, char separator = '\n') const;
        TdrError::ErrorType visualize(char* buffer, size_t size, size_t* usedSize = NULL,
                                      int indent = 0, char separator = '\n') const;
        const char* visualize_ex(TdrWriteBuf& destBuf, int indent = 0, char separator = '\n') const;
        const char* visualize_ex(char* buffer, size_t size, size_t* usedSize = NULL,
                                 int indent = 0, char separator = '\n') const;


    public: /* version infomation */
        enum
        {
            BASEVERSION = 1,
            CURRVERSION = 1,
        };
};

typedef       CSFrameActorCastSkill          tagCSFrameActorCastSkill;
typedef       CSFrameActorCastSkill             CSFRAMEACTORCASTSKILL;
typedef       CSFrameActorCastSkill*          LPCSFRAMEACTORCASTSKILL;
typedef const CSFrameActorCastSkill*         LCPCSFRAMEACTORCASTSKILL;


class CSFrameActorSkillEvent // 技能事件请求
{
    public: /* members */
        int32_t SkillGroupID; // 技能组id
        int8_t EventType; // 事件类型
        int32_t EventParam1; // 事件参数1
        CSPoint Param_Pos; // 位置参数
        CSPoint Param_Dir; // 方向参数

    public: /* methods */
        TdrError::ErrorType construct();

        TdrError::ErrorType pack(TdrWriteBuf& destBuf, unsigned cutVer = 0) const;
        TdrError::ErrorType pack(char* buffer, size_t size, size_t* usedSize = NULL, unsigned cutVer = 0) const;

        TdrError::ErrorType unpack(TdrReadBuf& srcBuf, unsigned cutVer = 0);
        TdrError::ErrorType unpack(const char* buffer, size_t size, size_t* usedSize = NULL, unsigned cutVer = 0);

        /* set indent = -1 to disable indent */
        TdrError::ErrorType visualize(TdrWriteBuf& destBuf, int indent = 0, char separator = '\n') const;
        TdrError::ErrorType visualize(char* buffer, size_t size, size_t* usedSize = NULL,
                                      int indent = 0, char separator = '\n') const;
        const char* visualize_ex(TdrWriteBuf& destBuf, int indent = 0, char separator = '\n') const;
        const char* visualize_ex(char* buffer, size_t size, size_t* usedSize = NULL,
                                 int indent = 0, char separator = '\n') const;


    public: /* version infomation */
        enum
        {
            BASEVERSION = 1,
            CURRVERSION = 1,
        };
};

typedef       CSFrameActorSkillEvent          tagCSFrameActorSkillEvent;
typedef       CSFrameActorSkillEvent             CSFRAMEACTORSKILLEVENT;
typedef       CSFrameActorSkillEvent*          LPCSFRAMEACTORSKILLEVENT;
typedef const CSFrameActorSkillEvent*         LCPCSFRAMEACTORSKILLEVENT;


class CSFrameActorAddSkillPoint // 技能加点请求
{
    public: /* members */
        char SkillBarType; // 技能栏类型

    public: /* methods */
        TdrError::ErrorType construct();

        TdrError::ErrorType pack(TdrWriteBuf& destBuf, unsigned cutVer = 0) const;
        TdrError::ErrorType pack(char* buffer, size_t size, size_t* usedSize = NULL, unsigned cutVer = 0) const;

        TdrError::ErrorType unpack(TdrReadBuf& srcBuf, unsigned cutVer = 0);
        TdrError::ErrorType unpack(const char* buffer, size_t size, size_t* usedSize = NULL, unsigned cutVer = 0);

        /* set indent = -1 to disable indent */
        TdrError::ErrorType visualize(TdrWriteBuf& destBuf, int indent = 0, char separator = '\n') const;
        TdrError::ErrorType visualize(char* buffer, size_t size, size_t* usedSize = NULL,
                                      int indent = 0, char separator = '\n') const;
        const char* visualize_ex(TdrWriteBuf& destBuf, int indent = 0, char separator = '\n') const;
        const char* visualize_ex(char* buffer, size_t size, size_t* usedSize = NULL,
                                 int indent = 0, char separator = '\n') const;


    public: /* version infomation */
        enum
        {
            BASEVERSION = 1,
            CURRVERSION = 1,
        };
};

typedef       CSFrameActorAddSkillPoint          tagCSFrameActorAddSkillPoint;
typedef       CSFrameActorAddSkillPoint             CSFRAMEACTORADDSKILLPOINT;
typedef       CSFrameActorAddSkillPoint*          LPCSFRAMEACTORADDSKILLPOINT;
typedef const CSFrameActorAddSkillPoint*         LCPCSFRAMEACTORADDSKILLPOINT;


class CSFrameActorEquipOp // 装备操作请求
{
    public: /* members */
        int32_t ObjectId; // 装备的ObjectID
        int8_t OpType; // 操作类型
        int8_t EquipBarType; // 装备栏类型

    public: /* methods */
        TdrError::ErrorType construct();

        TdrError::ErrorType pack(TdrWriteBuf& destBuf, unsigned cutVer = 0) const;
        TdrError::ErrorType pack(char* buffer, size_t size, size_t* usedSize = NULL, unsigned cutVer = 0) const;

        TdrError::ErrorType unpack(TdrReadBuf& srcBuf, unsigned cutVer = 0);
        TdrError::ErrorType unpack(const char* buffer, size_t size, size_t* usedSize = NULL, unsigned cutVer = 0);

        /* set indent = -1 to disable indent */
        TdrError::ErrorType visualize(TdrWriteBuf& destBuf, int indent = 0, char separator = '\n') const;
        TdrError::ErrorType visualize(char* buffer, size_t size, size_t* usedSize = NULL,
                                      int indent = 0, char separator = '\n') const;
        const char* visualize_ex(TdrWriteBuf& destBuf, int indent = 0, char separator = '\n') const;
        const char* visualize_ex(char* buffer, size_t size, size_t* usedSize = NULL,
                                 int indent = 0, char separator = '\n') const;


    public: /* version infomation */
        enum
        {
            BASEVERSION = 1,
            CURRVERSION = 1,
        };
};

typedef       CSFrameActorEquipOp          tagCSFrameActorEquipOp;
typedef       CSFrameActorEquipOp             CSFRAMEACTOREQUIPOP;
typedef       CSFrameActorEquipOp*          LPCSFRAMEACTOREQUIPOP;
typedef const CSFrameActorEquipOp*         LCPCSFRAMEACTOREQUIPOP;


class CSFrameBattleStartMove // 请求移动请求
{
    public: /* members */
        CSPoint TargetPos; // 目标位置
        CSPoint CurPos; // 位置
        CSPoint CurDir; // 朝向
        int16_t MoveDir_X; // 移动方向
        int16_t MoveDir_Z; // 移动方向
        int8_t OpMode; // 移动模式
        int8_t WalkFlag; // 走路状态

    public: /* methods */
        TdrError::ErrorType construct();

        TdrError::ErrorType pack(TdrWriteBuf& destBuf, unsigned cutVer = 0) const;
        TdrError::ErrorType pack(char* buffer, size_t size, size_t* usedSize = NULL, unsigned cutVer = 0) const;

        TdrError::ErrorType unpack(TdrReadBuf& srcBuf, unsigned cutVer = 0);
        TdrError::ErrorType unpack(const char* buffer, size_t size, size_t* usedSize = NULL, unsigned cutVer = 0);

        /* set indent = -1 to disable indent */
        TdrError::ErrorType visualize(TdrWriteBuf& destBuf, int indent = 0, char separator = '\n') const;
        TdrError::ErrorType visualize(char* buffer, size_t size, size_t* usedSize = NULL,
                                      int indent = 0, char separator = '\n') const;
        const char* visualize_ex(TdrWriteBuf& destBuf, int indent = 0, char separator = '\n') const;
        const char* visualize_ex(char* buffer, size_t size, size_t* usedSize = NULL,
                                 int indent = 0, char separator = '\n') const;


    public: /* version infomation */
        enum
        {
            BASEVERSION = 1,
            CURRVERSION = 1,
        };
};

typedef       CSFrameBattleStartMove          tagCSFrameBattleStartMove;
typedef       CSFrameBattleStartMove             CSFRAMEBATTLESTARTMOVE;
typedef       CSFrameBattleStartMove*          LPCSFRAMEBATTLESTARTMOVE;
typedef const CSFrameBattleStartMove*         LCPCSFRAMEBATTLESTARTMOVE;


class CSFrameBattleStopMove // 停止移动请求
{
    public: /* members */
        CSPoint CurPos; // 位置
        CSPoint CurDir; // 朝向

    public: /* methods */
        TdrError::ErrorType construct();

        TdrError::ErrorType pack(TdrWriteBuf& destBuf, unsigned cutVer = 0) const;
        TdrError::ErrorType pack(char* buffer, size_t size, size_t* usedSize = NULL, unsigned cutVer = 0) const;

        TdrError::ErrorType unpack(TdrReadBuf& srcBuf, unsigned cutVer = 0);
        TdrError::ErrorType unpack(const char* buffer, size_t size, size_t* usedSize = NULL, unsigned cutVer = 0);

        /* set indent = -1 to disable indent */
        TdrError::ErrorType visualize(TdrWriteBuf& destBuf, int indent = 0, char separator = '\n') const;
        TdrError::ErrorType visualize(char* buffer, size_t size, size_t* usedSize = NULL,
                                      int indent = 0, char separator = '\n') const;
        const char* visualize_ex(TdrWriteBuf& destBuf, int indent = 0, char separator = '\n') const;
        const char* visualize_ex(char* buffer, size_t size, size_t* usedSize = NULL,
                                 int indent = 0, char separator = '\n') const;


    public: /* version infomation */
        enum
        {
            BASEVERSION = 1,
            CURRVERSION = 1,
        };
};

typedef       CSFrameBattleStopMove          tagCSFrameBattleStopMove;
typedef       CSFrameBattleStopMove             CSFRAMEBATTLESTOPMOVE;
typedef       CSFrameBattleStopMove*          LPCSFRAMEBATTLESTOPMOVE;
typedef const CSFrameBattleStopMove*         LCPCSFRAMEBATTLESTOPMOVE;


class CSFrameBattleJump // 跳跃
{
    public: /* members */
        int8_t Reserved; // 保留字段

    public: /* methods */
        TdrError::ErrorType construct();

        TdrError::ErrorType pack(TdrWriteBuf& destBuf, unsigned cutVer = 0) const;
        TdrError::ErrorType pack(char* buffer, size_t size, size_t* usedSize = NULL, unsigned cutVer = 0) const;

        TdrError::ErrorType unpack(TdrReadBuf& srcBuf, unsigned cutVer = 0);
        TdrError::ErrorType unpack(const char* buffer, size_t size, size_t* usedSize = NULL, unsigned cutVer = 0);

        /* set indent = -1 to disable indent */
        TdrError::ErrorType visualize(TdrWriteBuf& destBuf, int indent = 0, char separator = '\n') const;
        TdrError::ErrorType visualize(char* buffer, size_t size, size_t* usedSize = NULL,
                                      int indent = 0, char separator = '\n') const;
        const char* visualize_ex(TdrWriteBuf& destBuf, int indent = 0, char separator = '\n') const;
        const char* visualize_ex(char* buffer, size_t size, size_t* usedSize = NULL,
                                 int indent = 0, char separator = '\n') const;


    public: /* version infomation */
        enum
        {
            BASEVERSION = 1,
            CURRVERSION = 1,
        };
};

typedef       CSFrameBattleJump          tagCSFrameBattleJump;
typedef       CSFrameBattleJump             CSFRAMEBATTLEJUMP;
typedef       CSFrameBattleJump*          LPCSFRAMEBATTLEJUMP;
typedef const CSFrameBattleJump*         LCPCSFRAMEBATTLEJUMP;


class CSFrameBattleWalk // 设置走路状态
{
    public: /* members */
        int8_t WalkFlag; // 走路状态

    public: /* methods */
        TdrError::ErrorType construct();

        TdrError::ErrorType pack(TdrWriteBuf& destBuf, unsigned cutVer = 0) const;
        TdrError::ErrorType pack(char* buffer, size_t size, size_t* usedSize = NULL, unsigned cutVer = 0) const;

        TdrError::ErrorType unpack(TdrReadBuf& srcBuf, unsigned cutVer = 0);
        TdrError::ErrorType unpack(const char* buffer, size_t size, size_t* usedSize = NULL, unsigned cutVer = 0);

        /* set indent = -1 to disable indent */
        TdrError::ErrorType visualize(TdrWriteBuf& destBuf, int indent = 0, char separator = '\n') const;
        TdrError::ErrorType visualize(char* buffer, size_t size, size_t* usedSize = NULL,
                                      int indent = 0, char separator = '\n') const;
        const char* visualize_ex(TdrWriteBuf& destBuf, int indent = 0, char separator = '\n') const;
        const char* visualize_ex(char* buffer, size_t size, size_t* usedSize = NULL,
                                 int indent = 0, char separator = '\n') const;


    public: /* version infomation */
        enum
        {
            BASEVERSION = 1,
            CURRVERSION = 1,
        };
};

typedef       CSFrameBattleWalk          tagCSFrameBattleWalk;
typedef       CSFrameBattleWalk             CSFRAMEBATTLEWALK;
typedef       CSFrameBattleWalk*          LPCSFRAMEBATTLEWALK;
typedef const CSFrameBattleWalk*         LCPCSFRAMEBATTLEWALK;


class CSFrameBattleFaceDir // 转向
{
    public: /* members */
        CSPoint CurDir; // 朝向

    public: /* methods */
        TdrError::ErrorType construct();

        TdrError::ErrorType pack(TdrWriteBuf& destBuf, unsigned cutVer = 0) const;
        TdrError::ErrorType pack(char* buffer, size_t size, size_t* usedSize = NULL, unsigned cutVer = 0) const;

        TdrError::ErrorType unpack(TdrReadBuf& srcBuf, unsigned cutVer = 0);
        TdrError::ErrorType unpack(const char* buffer, size_t size, size_t* usedSize = NULL, unsigned cutVer = 0);

        /* set indent = -1 to disable indent */
        TdrError::ErrorType visualize(TdrWriteBuf& destBuf, int indent = 0, char separator = '\n') const;
        TdrError::ErrorType visualize(char* buffer, size_t size, size_t* usedSize = NULL,
                                      int indent = 0, char separator = '\n') const;
        const char* visualize_ex(TdrWriteBuf& destBuf, int indent = 0, char separator = '\n') const;
        const char* visualize_ex(char* buffer, size_t size, size_t* usedSize = NULL,
                                 int indent = 0, char separator = '\n') const;


    public: /* version infomation */
        enum
        {
            BASEVERSION = 1,
            CURRVERSION = 1,
        };
};

typedef       CSFrameBattleFaceDir          tagCSFrameBattleFaceDir;
typedef       CSFrameBattleFaceDir             CSFRAMEBATTLEFACEDIR;
typedef       CSFrameBattleFaceDir*          LPCSFRAMEBATTLEFACEDIR;
typedef const CSFrameBattleFaceDir*         LCPCSFRAMEBATTLEFACEDIR;


class CSFrameBattleUseItem // 使用道具
{
    public: /* members */
        int8_t ItemType; // 道具类型
        int32_t ResID; // 道具资源id
        int8_t SkillItemBarType; // 技能道具栏
        int8_t ChangeNexSkillItem; // 是否切换下一个技能道具,1:是
        int32_t Param1; // 参数1
        int8_t InputMode; // 技能释放输入模式
        CSPoint TargetPos; // 目标位置
        int32_t TargetObjID; // 目标ObjID

    public: /* methods */
        TdrError::ErrorType construct();

        TdrError::ErrorType pack(TdrWriteBuf& destBuf, unsigned cutVer = 0) const;
        TdrError::ErrorType pack(char* buffer, size_t size, size_t* usedSize = NULL, unsigned cutVer = 0) const;

        TdrError::ErrorType unpack(TdrReadBuf& srcBuf, unsigned cutVer = 0);
        TdrError::ErrorType unpack(const char* buffer, size_t size, size_t* usedSize = NULL, unsigned cutVer = 0);

        /* set indent = -1 to disable indent */
        TdrError::ErrorType visualize(TdrWriteBuf& destBuf, int indent = 0, char separator = '\n') const;
        TdrError::ErrorType visualize(char* buffer, size_t size, size_t* usedSize = NULL,
                                      int indent = 0, char separator = '\n') const;
        const char* visualize_ex(TdrWriteBuf& destBuf, int indent = 0, char separator = '\n') const;
        const char* visualize_ex(char* buffer, size_t size, size_t* usedSize = NULL,
                                 int indent = 0, char separator = '\n') const;


    public: /* version infomation */
        enum
        {
            BASEVERSION = 1,
            CURRVERSION = 1,
        };
};

typedef       CSFrameBattleUseItem          tagCSFrameBattleUseItem;
typedef       CSFrameBattleUseItem             CSFRAMEBATTLEUSEITEM;
typedef       CSFrameBattleUseItem*          LPCSFRAMEBATTLEUSEITEM;
typedef const CSFrameBattleUseItem*         LCPCSFRAMEBATTLEUSEITEM;


class CSFrameBattleTeamConfig // 玩家游戏设置
{
    public: /* members */
        BattleTeamCreateSystemParam Systems[MAX_BATTLESYSTEM_COUNT]; // 系统设置

    public: /* methods */
        TdrError::ErrorType construct();

        TdrError::ErrorType pack(TdrWriteBuf& destBuf, unsigned cutVer = 0) const;
        TdrError::ErrorType pack(char* buffer, size_t size, size_t* usedSize = NULL, unsigned cutVer = 0) const;

        TdrError::ErrorType unpack(TdrReadBuf& srcBuf, unsigned cutVer = 0);
        TdrError::ErrorType unpack(const char* buffer, size_t size, size_t* usedSize = NULL, unsigned cutVer = 0);

        /* set indent = -1 to disable indent */
        TdrError::ErrorType visualize(TdrWriteBuf& destBuf, int indent = 0, char separator = '\n') const;
        TdrError::ErrorType visualize(char* buffer, size_t size, size_t* usedSize = NULL,
                                      int indent = 0, char separator = '\n') const;
        const char* visualize_ex(TdrWriteBuf& destBuf, int indent = 0, char separator = '\n') const;
        const char* visualize_ex(char* buffer, size_t size, size_t* usedSize = NULL,
                                 int indent = 0, char separator = '\n') const;


    public: /* version infomation */
        enum
        {
            BASEVERSION = 1,
            CURRVERSION = 1,
        };
};

typedef       CSFrameBattleTeamConfig          tagCSFrameBattleTeamConfig;
typedef       CSFrameBattleTeamConfig             CSFRAMEBATTLETEAMCONFIG;
typedef       CSFrameBattleTeamConfig*          LPCSFRAMEBATTLETEAMCONFIG;
typedef const CSFrameBattleTeamConfig*         LCPCSFRAMEBATTLETEAMCONFIG;


class CSFrameBattleEventNotify // 游戏事件通知
{
    public: /* members */
        int8_t EventType; // 事件类型
        int32_t Param1; // 事件参数1

    public: /* methods */
        TdrError::ErrorType construct();

        TdrError::ErrorType pack(TdrWriteBuf& destBuf, unsigned cutVer = 0) const;
        TdrError::ErrorType pack(char* buffer, size_t size, size_t* usedSize = NULL, unsigned cutVer = 0) const;

        TdrError::ErrorType unpack(TdrReadBuf& srcBuf, unsigned cutVer = 0);
        TdrError::ErrorType unpack(const char* buffer, size_t size, size_t* usedSize = NULL, unsigned cutVer = 0);

        /* set indent = -1 to disable indent */
        TdrError::ErrorType visualize(TdrWriteBuf& destBuf, int indent = 0, char separator = '\n') const;
        TdrError::ErrorType visualize(char* buffer, size_t size, size_t* usedSize = NULL,
                                      int indent = 0, char separator = '\n') const;
        const char* visualize_ex(TdrWriteBuf& destBuf, int indent = 0, char separator = '\n') const;
        const char* visualize_ex(char* buffer, size_t size, size_t* usedSize = NULL,
                                 int indent = 0, char separator = '\n') const;


    public: /* version infomation */
        enum
        {
            BASEVERSION = 1,
            CURRVERSION = 1,
        };
};

typedef       CSFrameBattleEventNotify          tagCSFrameBattleEventNotify;
typedef       CSFrameBattleEventNotify             CSFRAMEBATTLEEVENTNOTIFY;
typedef       CSFrameBattleEventNotify*          LPCSFRAMEBATTLEEVENTNOTIFY;
typedef const CSFrameBattleEventNotify*         LCPCSFRAMEBATTLEEVENTNOTIFY;


class CSFrameBattleGmCmd // gm指令
{
    public: /* members */
        int16_t cmdLen; // len
        char cmd[MAX_FRAME_CMD_LEN]; // cmd

    public: /* methods */
        TdrError::ErrorType construct();

        TdrError::ErrorType pack(TdrWriteBuf& destBuf, unsigned cutVer = 0) const;
        TdrError::ErrorType pack(char* buffer, size_t size, size_t* usedSize = NULL, unsigned cutVer = 0) const;

        TdrError::ErrorType unpack(TdrReadBuf& srcBuf, unsigned cutVer = 0);
        TdrError::ErrorType unpack(const char* buffer, size_t size, size_t* usedSize = NULL, unsigned cutVer = 0);

        /* set indent = -1 to disable indent */
        TdrError::ErrorType visualize(TdrWriteBuf& destBuf, int indent = 0, char separator = '\n') const;
        TdrError::ErrorType visualize(char* buffer, size_t size, size_t* usedSize = NULL,
                                      int indent = 0, char separator = '\n') const;
        const char* visualize_ex(TdrWriteBuf& destBuf, int indent = 0, char separator = '\n') const;
        const char* visualize_ex(char* buffer, size_t size, size_t* usedSize = NULL,
                                 int indent = 0, char separator = '\n') const;


    public: /* version infomation */
        enum
        {
            BASEVERSION = 1,
            CURRVERSION = 1,
        };
};

typedef       CSFrameBattleGmCmd          tagCSFrameBattleGmCmd;
typedef       CSFrameBattleGmCmd             CSFRAMEBATTLEGMCMD;
typedef       CSFrameBattleGmCmd*          LPCSFRAMEBATTLEGMCMD;
typedef const CSFrameBattleGmCmd*         LCPCSFRAMEBATTLEGMCMD;


class CSFrameBattleGmScaleTime // gm指令修改服务器时间缩放
{
    public: /* members */
        int16_t ScalePercent; // 时间缩放比例百分比

    public: /* methods */
        TdrError::ErrorType construct();

        TdrError::ErrorType pack(TdrWriteBuf& destBuf, unsigned cutVer = 0) const;
        TdrError::ErrorType pack(char* buffer, size_t size, size_t* usedSize = NULL, unsigned cutVer = 0) const;

        TdrError::ErrorType unpack(TdrReadBuf& srcBuf, unsigned cutVer = 0);
        TdrError::ErrorType unpack(const char* buffer, size_t size, size_t* usedSize = NULL, unsigned cutVer = 0);

        /* set indent = -1 to disable indent */
        TdrError::ErrorType visualize(TdrWriteBuf& destBuf, int indent = 0, char separator = '\n') const;
        TdrError::ErrorType visualize(char* buffer, size_t size, size_t* usedSize = NULL,
                                      int indent = 0, char separator = '\n') const;
        const char* visualize_ex(TdrWriteBuf& destBuf, int indent = 0, char separator = '\n') const;
        const char* visualize_ex(char* buffer, size_t size, size_t* usedSize = NULL,
                                 int indent = 0, char separator = '\n') const;


    public: /* version infomation */
        enum
        {
            BASEVERSION = 1,
            CURRVERSION = 1,
        };
};

typedef       CSFrameBattleGmScaleTime          tagCSFrameBattleGmScaleTime;
typedef       CSFrameBattleGmScaleTime             CSFRAMEBATTLEGMSCALETIME;
typedef       CSFrameBattleGmScaleTime*          LPCSFRAMEBATTLEGMSCALETIME;
typedef const CSFrameBattleGmScaleTime*         LCPCSFRAMEBATTLEGMSCALETIME;


class CSFrameBattleSetHotbarIndex // 切换快捷栏
{
    public: /* members */
        int32_t hotbarIndex; // 快捷栏索引

    public: /* methods */
        TdrError::ErrorType construct();

        TdrError::ErrorType pack(TdrWriteBuf& destBuf, unsigned cutVer = 0) const;
        TdrError::ErrorType pack(char* buffer, size_t size, size_t* usedSize = NULL, unsigned cutVer = 0) const;

        TdrError::ErrorType unpack(TdrReadBuf& srcBuf, unsigned cutVer = 0);
        TdrError::ErrorType unpack(const char* buffer, size_t size, size_t* usedSize = NULL, unsigned cutVer = 0);

        /* set indent = -1 to disable indent */
        TdrError::ErrorType visualize(TdrWriteBuf& destBuf, int indent = 0, char separator = '\n') const;
        TdrError::ErrorType visualize(char* buffer, size_t size, size_t* usedSize = NULL,
                                      int indent = 0, char separator = '\n') const;
        const char* visualize_ex(TdrWriteBuf& destBuf, int indent = 0, char separator = '\n') const;
        const char* visualize_ex(char* buffer, size_t size, size_t* usedSize = NULL,
                                 int indent = 0, char separator = '\n') const;


    public: /* version infomation */
        enum
        {
            BASEVERSION = 1,
            CURRVERSION = 1,
        };
};

typedef       CSFrameBattleSetHotbarIndex          tagCSFrameBattleSetHotbarIndex;
typedef       CSFrameBattleSetHotbarIndex             CSFRAMEBATTLESETHOTBARINDEX;
typedef       CSFrameBattleSetHotbarIndex*          LPCSFRAMEBATTLESETHOTBARINDEX;
typedef const CSFrameBattleSetHotbarIndex*         LCPCSFRAMEBATTLESETHOTBARINDEX;


class CSFrameBattleSwapItemSlot // 物品栏交换
{
    public: /* members */
        char key1[MAX_GM_LEN]; // 1槽位key
        int16_t key1Len; // key1长度
        int32_t index1; // 1槽位index
        char key2[MAX_GM_LEN]; // 2槽位key
        int16_t key2Len; // key2长度
        int32_t index2; // 2槽位index

    public: /* methods */
        TdrError::ErrorType construct();

        TdrError::ErrorType pack(TdrWriteBuf& destBuf, unsigned cutVer = 0) const;
        TdrError::ErrorType pack(char* buffer, size_t size, size_t* usedSize = NULL, unsigned cutVer = 0) const;

        TdrError::ErrorType unpack(TdrReadBuf& srcBuf, unsigned cutVer = 0);
        TdrError::ErrorType unpack(const char* buffer, size_t size, size_t* usedSize = NULL, unsigned cutVer = 0);

        /* set indent = -1 to disable indent */
        TdrError::ErrorType visualize(TdrWriteBuf& destBuf, int indent = 0, char separator = '\n') const;
        TdrError::ErrorType visualize(char* buffer, size_t size, size_t* usedSize = NULL,
                                      int indent = 0, char separator = '\n') const;
        const char* visualize_ex(TdrWriteBuf& destBuf, int indent = 0, char separator = '\n') const;
        const char* visualize_ex(char* buffer, size_t size, size_t* usedSize = NULL,
                                 int indent = 0, char separator = '\n') const;


    public: /* version infomation */
        enum
        {
            BASEVERSION = 1,
            CURRVERSION = 1,
        };
};

typedef       CSFrameBattleSwapItemSlot          tagCSFrameBattleSwapItemSlot;
typedef       CSFrameBattleSwapItemSlot             CSFRAMEBATTLESWAPITEMSLOT;
typedef       CSFrameBattleSwapItemSlot*          LPCSFRAMEBATTLESWAPITEMSLOT;
typedef const CSFrameBattleSwapItemSlot*         LCPCSFRAMEBATTLESWAPITEMSLOT;


class CSFrameBattleGameEndNotify // 服务器主动结束战斗通知
{
    public: /* members */
        int8_t WinerLegionType; // 获胜方阵营

    public: /* methods */
        TdrError::ErrorType construct();

        TdrError::ErrorType pack(TdrWriteBuf& destBuf, unsigned cutVer = 0) const;
        TdrError::ErrorType pack(char* buffer, size_t size, size_t* usedSize = NULL, unsigned cutVer = 0) const;

        TdrError::ErrorType unpack(TdrReadBuf& srcBuf, unsigned cutVer = 0);
        TdrError::ErrorType unpack(const char* buffer, size_t size, size_t* usedSize = NULL, unsigned cutVer = 0);

        /* set indent = -1 to disable indent */
        TdrError::ErrorType visualize(TdrWriteBuf& destBuf, int indent = 0, char separator = '\n') const;
        TdrError::ErrorType visualize(char* buffer, size_t size, size_t* usedSize = NULL,
                                      int indent = 0, char separator = '\n') const;
        const char* visualize_ex(TdrWriteBuf& destBuf, int indent = 0, char separator = '\n') const;
        const char* visualize_ex(char* buffer, size_t size, size_t* usedSize = NULL,
                                 int indent = 0, char separator = '\n') const;


    public: /* version infomation */
        enum
        {
            BASEVERSION = 1,
            CURRVERSION = 1,
        };
};

typedef       CSFrameBattleGameEndNotify          tagCSFrameBattleGameEndNotify;
typedef       CSFrameBattleGameEndNotify             CSFRAMEBATTLEGAMEENDNOTIFY;
typedef       CSFrameBattleGameEndNotify*          LPCSFRAMEBATTLEGAMEENDNOTIFY;
typedef const CSFrameBattleGameEndNotify*         LCPCSFRAMEBATTLEGAMEENDNOTIFY;


class CSFrameCmdData
{
    public: /* members */
        union
        {
            CSFrameActorCastSkill ActorCastSkill; // 单位释放技能
            CSFrameActorSkillEvent ActorSkillEvent; // 单位技能释放事件
            CSFrameActorAddSkillPoint ActorAddSkillPoint; // 单位技能加点
            CSFrameActorEquipOp ActorEquipOp; // 单位装备操作
            CSFrameBattleStartMove BattleStartMove; // 开始移动
            CSFrameBattleStopMove BattleStopMove; // 停止移动
            CSFrameBattleJump BattleJump; // 跳跃
            CSFrameBattleWalk BattleWalk; // 走跑切换
            CSFrameBattleFaceDir BattleFaceDir; // 转向
            CSFrameBattleTeamConfig BattleTeamConfig; // 同步玩家游戏设置
            CSFrameBattleEventNotify BattleEventNotify; // 游戏通用事件通知
            CSFrameBattleSetHotbarIndex BattleSetHotbarIndex; // 切换快捷栏
            CSFrameBattleSwapItemSlot BattleSwapItemSlot; // 物品栏交换
            CSFrameBattleGmCmd BattleGmCmd; // gm指令
            CSFrameBattleGmScaleTime BattleGmScaleTime; // gm指令修改服务器时间缩放
            CSFrameBattleGameEndNotify BattleGameEndNotify; // 服务器主动结束战斗通知
        };

    public: /* methods */
        TdrError::ErrorType construct(int64_t selector);

        TdrError::ErrorType pack(int64_t selector, TdrWriteBuf& destBuf, unsigned cutVer = 0) const;
        TdrError::ErrorType pack(int64_t selector, char* buffer, size_t size,
                                 size_t* usedSize = NULL, unsigned cutVer = 0) const;

        TdrError::ErrorType unpack(int64_t selector, TdrReadBuf& srcBuf, unsigned cutVer = 0);
        TdrError::ErrorType unpack(int64_t selector, const char* buffer, size_t size,
                                   size_t* usedSize = NULL, unsigned cutVer = 0);

        TdrError::ErrorType visualize(int64_t selector, TdrWriteBuf& destBuf,
                                      int indent = 0, char separator = '\n') const;
        TdrError::ErrorType visualize(int64_t selector, char* buffer, size_t size,
                                      size_t* usedSize = NULL, int indent = 0, char separator = '\n') const;


    public: /* version infomation */
        enum
        {
            BASEVERSION = 1,
            CURRVERSION = 1,
        };
};

typedef       CSFrameCmdData          tagCSFrameCmdData;
typedef       CSFrameCmdData             CSFRAMECMDDATA;
typedef       CSFrameCmdData*          LPCSFRAMECMDDATA;
typedef const CSFrameCmdData*         LCPCSFRAMECMDDATA;


class CSFrameCmdReq // 帧同步指令请求
{
    public: /* members */
        int32_t ObjID; // 对象ID
        int8_t TeamType; // 队伍类型
        char EventType; // 指令类型
        CSFrameCmdData CmdData; // 数据

    public: /* methods */
        TdrError::ErrorType construct();

        TdrError::ErrorType pack(TdrWriteBuf& destBuf, unsigned cutVer = 0) const;
        TdrError::ErrorType pack(char* buffer, size_t size, size_t* usedSize = NULL, unsigned cutVer = 0) const;

        TdrError::ErrorType unpack(TdrReadBuf& srcBuf, unsigned cutVer = 0);
        TdrError::ErrorType unpack(const char* buffer, size_t size, size_t* usedSize = NULL, unsigned cutVer = 0);

        /* set indent = -1 to disable indent */
        TdrError::ErrorType visualize(TdrWriteBuf& destBuf, int indent = 0, char separator = '\n') const;
        TdrError::ErrorType visualize(char* buffer, size_t size, size_t* usedSize = NULL,
                                      int indent = 0, char separator = '\n') const;
        const char* visualize_ex(TdrWriteBuf& destBuf, int indent = 0, char separator = '\n') const;
        const char* visualize_ex(char* buffer, size_t size, size_t* usedSize = NULL,
                                 int indent = 0, char separator = '\n') const;


    public: /* version infomation */
        enum
        {
            BASEVERSION = 1,
            CURRVERSION = 1,
        };
};

typedef       CSFrameCmdReq          tagCSFrameCmdReq;
typedef       CSFrameCmdReq             CSFRAMECMDREQ;
typedef       CSFrameCmdReq*          LPCSFRAMECMDREQ;
typedef const CSFrameCmdReq*         LCPCSFRAMECMDREQ;


class CSActorFrameEvent // 序列号的指令
{
    public: /* members */
        CSFrameCmdReq FrameCmd; // 指令序列数组

    public: /* methods */
        TdrError::ErrorType construct();

        TdrError::ErrorType pack(TdrWriteBuf& destBuf, unsigned cutVer = 0) const;
        TdrError::ErrorType pack(char* buffer, size_t size, size_t* usedSize = NULL, unsigned cutVer = 0) const;

        TdrError::ErrorType unpack(TdrReadBuf& srcBuf, unsigned cutVer = 0);
        TdrError::ErrorType unpack(const char* buffer, size_t size, size_t* usedSize = NULL, unsigned cutVer = 0);

        /* set indent = -1 to disable indent */
        TdrError::ErrorType visualize(TdrWriteBuf& destBuf, int indent = 0, char separator = '\n') const;
        TdrError::ErrorType visualize(char* buffer, size_t size, size_t* usedSize = NULL,
                                      int indent = 0, char separator = '\n') const;
        const char* visualize_ex(TdrWriteBuf& destBuf, int indent = 0, char separator = '\n') const;
        const char* visualize_ex(char* buffer, size_t size, size_t* usedSize = NULL,
                                 int indent = 0, char separator = '\n') const;


    public: /* version infomation */
        enum
        {
            BASEVERSION = 1,
            CURRVERSION = 1,
        };
};

typedef       CSActorFrameEvent          tagCSActorFrameEvent;
typedef       CSActorFrameEvent             CSACTORFRAMEEVENT;
typedef       CSActorFrameEvent*          LPCSACTORFRAMEEVENT;
typedef const CSActorFrameEvent*         LCPCSACTORFRAMEEVENT;


class CSBattleFrameEventNotify // 1帧事件广播
{
    public: /* members */
        int32_t Frame; // 服务器当前帧
        int8_t CmdCnt; // 指令个数
        CSActorFrameEvent CmdList[MAX_BATTLEFRAMECMD_MAXCNT]; // 指令列表

    public: /* methods */
        TdrError::ErrorType construct();

        TdrError::ErrorType pack(TdrWriteBuf& destBuf, unsigned cutVer = 0) const;
        TdrError::ErrorType pack(char* buffer, size_t size, size_t* usedSize = NULL, unsigned cutVer = 0) const;

        TdrError::ErrorType unpack(TdrReadBuf& srcBuf, unsigned cutVer = 0);
        TdrError::ErrorType unpack(const char* buffer, size_t size, size_t* usedSize = NULL, unsigned cutVer = 0);

        /* set indent = -1 to disable indent */
        TdrError::ErrorType visualize(TdrWriteBuf& destBuf, int indent = 0, char separator = '\n') const;
        TdrError::ErrorType visualize(char* buffer, size_t size, size_t* usedSize = NULL,
                                      int indent = 0, char separator = '\n') const;
        const char* visualize_ex(TdrWriteBuf& destBuf, int indent = 0, char separator = '\n') const;
        const char* visualize_ex(char* buffer, size_t size, size_t* usedSize = NULL,
                                 int indent = 0, char separator = '\n') const;


    public: /* version infomation */
        enum
        {
            BASEVERSION = 1,
            CURRVERSION = 1,
        };
};

typedef       CSBattleFrameEventNotify          tagCSBattleFrameEventNotify;
typedef       CSBattleFrameEventNotify             CSBATTLEFRAMEEVENTNOTIFY;
typedef       CSBattleFrameEventNotify*          LPCSBATTLEFRAMEEVENTNOTIFY;
typedef const CSBattleFrameEventNotify*         LCPCSBATTLEFRAMEEVENTNOTIFY;


class CSMiniMapEventReq
{
    public: /* members */
        int8_t Event; // 事件类型
        CSPoint TargetPos; // 目标位置

    public: /* methods */
        TdrError::ErrorType construct();

        TdrError::ErrorType pack(TdrWriteBuf& destBuf, unsigned cutVer = 0) const;
        TdrError::ErrorType pack(char* buffer, size_t size, size_t* usedSize = NULL, unsigned cutVer = 0) const;

        TdrError::ErrorType unpack(TdrReadBuf& srcBuf, unsigned cutVer = 0);
        TdrError::ErrorType unpack(const char* buffer, size_t size, size_t* usedSize = NULL, unsigned cutVer = 0);

        /* set indent = -1 to disable indent */
        TdrError::ErrorType visualize(TdrWriteBuf& destBuf, int indent = 0, char separator = '\n') const;
        TdrError::ErrorType visualize(char* buffer, size_t size, size_t* usedSize = NULL,
                                      int indent = 0, char separator = '\n') const;
        const char* visualize_ex(TdrWriteBuf& destBuf, int indent = 0, char separator = '\n') const;
        const char* visualize_ex(char* buffer, size_t size, size_t* usedSize = NULL,
                                 int indent = 0, char separator = '\n') const;


    public: /* version infomation */
        enum
        {
            BASEVERSION = 1,
            CURRVERSION = 1,
        };
};

typedef       CSMiniMapEventReq          tagCSMiniMapEventReq;
typedef       CSMiniMapEventReq             CSMINIMAPEVENTREQ;
typedef       CSMiniMapEventReq*          LPCSMINIMAPEVENTREQ;
typedef const CSMiniMapEventReq*         LCPCSMINIMAPEVENTREQ;


class CSMiniMapEventNotify
{
    public: /* members */
        int32_t PlayerID; // 发出玩家ID
        int8_t Event; // 事件类型
        CSPoint TargetPos; // 目标位置

    public: /* methods */
        TdrError::ErrorType construct();

        TdrError::ErrorType pack(TdrWriteBuf& destBuf, unsigned cutVer = 0) const;
        TdrError::ErrorType pack(char* buffer, size_t size, size_t* usedSize = NULL, unsigned cutVer = 0) const;

        TdrError::ErrorType unpack(TdrReadBuf& srcBuf, unsigned cutVer = 0);
        TdrError::ErrorType unpack(const char* buffer, size_t size, size_t* usedSize = NULL, unsigned cutVer = 0);

        /* set indent = -1 to disable indent */
        TdrError::ErrorType visualize(TdrWriteBuf& destBuf, int indent = 0, char separator = '\n') const;
        TdrError::ErrorType visualize(char* buffer, size_t size, size_t* usedSize = NULL,
                                      int indent = 0, char separator = '\n') const;
        const char* visualize_ex(TdrWriteBuf& destBuf, int indent = 0, char separator = '\n') const;
        const char* visualize_ex(char* buffer, size_t size, size_t* usedSize = NULL,
                                 int indent = 0, char separator = '\n') const;


    public: /* version infomation */
        enum
        {
            BASEVERSION = 1,
            CURRVERSION = 1,
        };
};

typedef       CSMiniMapEventNotify          tagCSMiniMapEventNotify;
typedef       CSMiniMapEventNotify             CSMINIMAPEVENTNOTIFY;
typedef       CSMiniMapEventNotify*          LPCSMINIMAPEVENTNOTIFY;
typedef const CSMiniMapEventNotify*         LCPCSMINIMAPEVENTNOTIFY;


class CSSurrenderReq
{
    public: /* members */
        uint8_t Reserved; // 保留字段

    public: /* methods */
        TdrError::ErrorType construct();

        TdrError::ErrorType pack(TdrWriteBuf& destBuf, unsigned cutVer = 0) const;
        TdrError::ErrorType pack(char* buffer, size_t size, size_t* usedSize = NULL, unsigned cutVer = 0) const;

        TdrError::ErrorType unpack(TdrReadBuf& srcBuf, unsigned cutVer = 0);
        TdrError::ErrorType unpack(const char* buffer, size_t size, size_t* usedSize = NULL, unsigned cutVer = 0);

        /* set indent = -1 to disable indent */
        TdrError::ErrorType visualize(TdrWriteBuf& destBuf, int indent = 0, char separator = '\n') const;
        TdrError::ErrorType visualize(char* buffer, size_t size, size_t* usedSize = NULL,
                                      int indent = 0, char separator = '\n') const;
        const char* visualize_ex(TdrWriteBuf& destBuf, int indent = 0, char separator = '\n') const;
        const char* visualize_ex(char* buffer, size_t size, size_t* usedSize = NULL,
                                 int indent = 0, char separator = '\n') const;


    public: /* version infomation */
        enum
        {
            BASEVERSION = 1,
            CURRVERSION = 1,
        };
};

typedef       CSSurrenderReq          tagCSSurrenderReq;
typedef       CSSurrenderReq             CSSURRENDERREQ;
typedef       CSSurrenderReq*          LPCSSURRENDERREQ;
typedef const CSSurrenderReq*         LCPCSSURRENDERREQ;


class CSSurrenderAgree
{
    public: /* members */
        uint8_t Reserved; // 保留字段

    public: /* methods */
        TdrError::ErrorType construct();

        TdrError::ErrorType pack(TdrWriteBuf& destBuf, unsigned cutVer = 0) const;
        TdrError::ErrorType pack(char* buffer, size_t size, size_t* usedSize = NULL, unsigned cutVer = 0) const;

        TdrError::ErrorType unpack(TdrReadBuf& srcBuf, unsigned cutVer = 0);
        TdrError::ErrorType unpack(const char* buffer, size_t size, size_t* usedSize = NULL, unsigned cutVer = 0);

        /* set indent = -1 to disable indent */
        TdrError::ErrorType visualize(TdrWriteBuf& destBuf, int indent = 0, char separator = '\n') const;
        TdrError::ErrorType visualize(char* buffer, size_t size, size_t* usedSize = NULL,
                                      int indent = 0, char separator = '\n') const;
        const char* visualize_ex(TdrWriteBuf& destBuf, int indent = 0, char separator = '\n') const;
        const char* visualize_ex(char* buffer, size_t size, size_t* usedSize = NULL,
                                 int indent = 0, char separator = '\n') const;


    public: /* version infomation */
        enum
        {
            BASEVERSION = 1,
            CURRVERSION = 1,
        };
};

typedef       CSSurrenderAgree          tagCSSurrenderAgree;
typedef       CSSurrenderAgree             CSSURRENDERAGREE;
typedef       CSSurrenderAgree*          LPCSSURRENDERAGREE;
typedef const CSSurrenderAgree*         LCPCSSURRENDERAGREE;


class CSSurrenderRefuse
{
    public: /* members */
        uint8_t Reserved; // 保留字段

    public: /* methods */
        TdrError::ErrorType construct();

        TdrError::ErrorType pack(TdrWriteBuf& destBuf, unsigned cutVer = 0) const;
        TdrError::ErrorType pack(char* buffer, size_t size, size_t* usedSize = NULL, unsigned cutVer = 0) const;

        TdrError::ErrorType unpack(TdrReadBuf& srcBuf, unsigned cutVer = 0);
        TdrError::ErrorType unpack(const char* buffer, size_t size, size_t* usedSize = NULL, unsigned cutVer = 0);

        /* set indent = -1 to disable indent */
        TdrError::ErrorType visualize(TdrWriteBuf& destBuf, int indent = 0, char separator = '\n') const;
        TdrError::ErrorType visualize(char* buffer, size_t size, size_t* usedSize = NULL,
                                      int indent = 0, char separator = '\n') const;
        const char* visualize_ex(TdrWriteBuf& destBuf, int indent = 0, char separator = '\n') const;
        const char* visualize_ex(char* buffer, size_t size, size_t* usedSize = NULL,
                                 int indent = 0, char separator = '\n') const;


    public: /* version infomation */
        enum
        {
            BASEVERSION = 1,
            CURRVERSION = 1,
        };
};

typedef       CSSurrenderRefuse          tagCSSurrenderRefuse;
typedef       CSSurrenderRefuse             CSSURRENDERREFUSE;
typedef       CSSurrenderRefuse*          LPCSSURRENDERREFUSE;
typedef const CSSurrenderRefuse*         LCPCSSURRENDERREFUSE;


class CSSurrenderAgreeNotify
{
    public: /* members */
        int32_t PlayerID; // 发出玩家ID

    public: /* methods */
        TdrError::ErrorType construct();

        TdrError::ErrorType pack(TdrWriteBuf& destBuf, unsigned cutVer = 0) const;
        TdrError::ErrorType pack(char* buffer, size_t size, size_t* usedSize = NULL, unsigned cutVer = 0) const;

        TdrError::ErrorType unpack(TdrReadBuf& srcBuf, unsigned cutVer = 0);
        TdrError::ErrorType unpack(const char* buffer, size_t size, size_t* usedSize = NULL, unsigned cutVer = 0);

        /* set indent = -1 to disable indent */
        TdrError::ErrorType visualize(TdrWriteBuf& destBuf, int indent = 0, char separator = '\n') const;
        TdrError::ErrorType visualize(char* buffer, size_t size, size_t* usedSize = NULL,
                                      int indent = 0, char separator = '\n') const;
        const char* visualize_ex(TdrWriteBuf& destBuf, int indent = 0, char separator = '\n') const;
        const char* visualize_ex(char* buffer, size_t size, size_t* usedSize = NULL,
                                 int indent = 0, char separator = '\n') const;


    public: /* version infomation */
        enum
        {
            BASEVERSION = 1,
            CURRVERSION = 1,
        };
};

typedef       CSSurrenderAgreeNotify          tagCSSurrenderAgreeNotify;
typedef       CSSurrenderAgreeNotify             CSSURRENDERAGREENOTIFY;
typedef       CSSurrenderAgreeNotify*          LPCSSURRENDERAGREENOTIFY;
typedef const CSSurrenderAgreeNotify*         LCPCSSURRENDERAGREENOTIFY;


class CSSurrenderRefuseNotify
{
    public: /* members */
        int32_t PlayerID; // 发出玩家ID

    public: /* methods */
        TdrError::ErrorType construct();

        TdrError::ErrorType pack(TdrWriteBuf& destBuf, unsigned cutVer = 0) const;
        TdrError::ErrorType pack(char* buffer, size_t size, size_t* usedSize = NULL, unsigned cutVer = 0) const;

        TdrError::ErrorType unpack(TdrReadBuf& srcBuf, unsigned cutVer = 0);
        TdrError::ErrorType unpack(const char* buffer, size_t size, size_t* usedSize = NULL, unsigned cutVer = 0);

        /* set indent = -1 to disable indent */
        TdrError::ErrorType visualize(TdrWriteBuf& destBuf, int indent = 0, char separator = '\n') const;
        TdrError::ErrorType visualize(char* buffer, size_t size, size_t* usedSize = NULL,
                                      int indent = 0, char separator = '\n') const;
        const char* visualize_ex(TdrWriteBuf& destBuf, int indent = 0, char separator = '\n') const;
        const char* visualize_ex(char* buffer, size_t size, size_t* usedSize = NULL,
                                 int indent = 0, char separator = '\n') const;


    public: /* version infomation */
        enum
        {
            BASEVERSION = 1,
            CURRVERSION = 1,
        };
};

typedef       CSSurrenderRefuseNotify          tagCSSurrenderRefuseNotify;
typedef       CSSurrenderRefuseNotify             CSSURRENDERREFUSENOTIFY;
typedef       CSSurrenderRefuseNotify*          LPCSSURRENDERREFUSENOTIFY;
typedef const CSSurrenderRefuseNotify*         LCPCSSURRENDERREFUSENOTIFY;


class CSSurrenderResult
{
    public: /* members */
        int8_t LoseLegionType; // 阵营类型

    public: /* methods */
        TdrError::ErrorType construct();

        TdrError::ErrorType pack(TdrWriteBuf& destBuf, unsigned cutVer = 0) const;
        TdrError::ErrorType pack(char* buffer, size_t size, size_t* usedSize = NULL, unsigned cutVer = 0) const;

        TdrError::ErrorType unpack(TdrReadBuf& srcBuf, unsigned cutVer = 0);
        TdrError::ErrorType unpack(const char* buffer, size_t size, size_t* usedSize = NULL, unsigned cutVer = 0);

        /* set indent = -1 to disable indent */
        TdrError::ErrorType visualize(TdrWriteBuf& destBuf, int indent = 0, char separator = '\n') const;
        TdrError::ErrorType visualize(char* buffer, size_t size, size_t* usedSize = NULL,
                                      int indent = 0, char separator = '\n') const;
        const char* visualize_ex(TdrWriteBuf& destBuf, int indent = 0, char separator = '\n') const;
        const char* visualize_ex(char* buffer, size_t size, size_t* usedSize = NULL,
                                 int indent = 0, char separator = '\n') const;


    public: /* version infomation */
        enum
        {
            BASEVERSION = 1,
            CURRVERSION = 1,
        };
};

typedef       CSSurrenderResult          tagCSSurrenderResult;
typedef       CSSurrenderResult             CSSURRENDERRESULT;
typedef       CSSurrenderResult*          LPCSSURRENDERRESULT;
typedef const CSSurrenderResult*         LCPCSSURRENDERRESULT;


class CSSurrenderReqNotify
{
    public: /* members */
        int32_t PlayerID; // 发出玩家ID

    public: /* methods */
        TdrError::ErrorType construct();

        TdrError::ErrorType pack(TdrWriteBuf& destBuf, unsigned cutVer = 0) const;
        TdrError::ErrorType pack(char* buffer, size_t size, size_t* usedSize = NULL, unsigned cutVer = 0) const;

        TdrError::ErrorType unpack(TdrReadBuf& srcBuf, unsigned cutVer = 0);
        TdrError::ErrorType unpack(const char* buffer, size_t size, size_t* usedSize = NULL, unsigned cutVer = 0);

        /* set indent = -1 to disable indent */
        TdrError::ErrorType visualize(TdrWriteBuf& destBuf, int indent = 0, char separator = '\n') const;
        TdrError::ErrorType visualize(char* buffer, size_t size, size_t* usedSize = NULL,
                                      int indent = 0, char separator = '\n') const;
        const char* visualize_ex(TdrWriteBuf& destBuf, int indent = 0, char separator = '\n') const;
        const char* visualize_ex(char* buffer, size_t size, size_t* usedSize = NULL,
                                 int indent = 0, char separator = '\n') const;


    public: /* version infomation */
        enum
        {
            BASEVERSION = 1,
            CURRVERSION = 1,
        };
};

typedef       CSSurrenderReqNotify          tagCSSurrenderReqNotify;
typedef       CSSurrenderReqNotify             CSSURRENDERREQNOTIFY;
typedef       CSSurrenderReqNotify*          LPCSSURRENDERREQNOTIFY;
typedef const CSSurrenderReqNotify*         LCPCSSURRENDERREQNOTIFY;


class CSFetchFrameDataReq
{
    public: /* members */
        uint8_t Finish; // 是否结束

    public: /* methods */
        TdrError::ErrorType construct();

        TdrError::ErrorType pack(TdrWriteBuf& destBuf, unsigned cutVer = 0) const;
        TdrError::ErrorType pack(char* buffer, size_t size, size_t* usedSize = NULL, unsigned cutVer = 0) const;

        TdrError::ErrorType unpack(TdrReadBuf& srcBuf, unsigned cutVer = 0);
        TdrError::ErrorType unpack(const char* buffer, size_t size, size_t* usedSize = NULL, unsigned cutVer = 0);

        /* set indent = -1 to disable indent */
        TdrError::ErrorType visualize(TdrWriteBuf& destBuf, int indent = 0, char separator = '\n') const;
        TdrError::ErrorType visualize(char* buffer, size_t size, size_t* usedSize = NULL,
                                      int indent = 0, char separator = '\n') const;
        const char* visualize_ex(TdrWriteBuf& destBuf, int indent = 0, char separator = '\n') const;
        const char* visualize_ex(char* buffer, size_t size, size_t* usedSize = NULL,
                                 int indent = 0, char separator = '\n') const;


    public: /* version infomation */
        enum
        {
            BASEVERSION = 1,
            CURRVERSION = 1,
        };
};

typedef       CSFetchFrameDataReq          tagCSFetchFrameDataReq;
typedef       CSFetchFrameDataReq             CSFETCHFRAMEDATAREQ;
typedef       CSFetchFrameDataReq*          LPCSFETCHFRAMEDATAREQ;
typedef const CSFetchFrameDataReq*         LCPCSFETCHFRAMEDATAREQ;


class CSPlayerConnectNotify
{
    public: /* members */
        int32_t PlayerID; // 玩家ID
        uint8_t Connect; // 是否连接服务器

    public: /* methods */
        TdrError::ErrorType construct();

        TdrError::ErrorType pack(TdrWriteBuf& destBuf, unsigned cutVer = 0) const;
        TdrError::ErrorType pack(char* buffer, size_t size, size_t* usedSize = NULL, unsigned cutVer = 0) const;

        TdrError::ErrorType unpack(TdrReadBuf& srcBuf, unsigned cutVer = 0);
        TdrError::ErrorType unpack(const char* buffer, size_t size, size_t* usedSize = NULL, unsigned cutVer = 0);

        /* set indent = -1 to disable indent */
        TdrError::ErrorType visualize(TdrWriteBuf& destBuf, int indent = 0, char separator = '\n') const;
        TdrError::ErrorType visualize(char* buffer, size_t size, size_t* usedSize = NULL,
                                      int indent = 0, char separator = '\n') const;
        const char* visualize_ex(TdrWriteBuf& destBuf, int indent = 0, char separator = '\n') const;
        const char* visualize_ex(char* buffer, size_t size, size_t* usedSize = NULL,
                                 int indent = 0, char separator = '\n') const;


    public: /* version infomation */
        enum
        {
            BASEVERSION = 1,
            CURRVERSION = 1,
        };
};

typedef       CSPlayerConnectNotify          tagCSPlayerConnectNotify;
typedef       CSPlayerConnectNotify             CSPLAYERCONNECTNOTIFY;
typedef       CSPlayerConnectNotify*          LPCSPLAYERCONNECTNOTIFY;
typedef const CSPlayerConnectNotify*         LCPCSPLAYERCONNECTNOTIFY;


class CSReplaySvrNewBattleReq
{
    public: /* members */
        int32_t BsRoomID; // 房间id
        BattleCreateParams params; // 战场创建信息

    public: /* methods */
        TdrError::ErrorType construct();

        TdrError::ErrorType pack(TdrWriteBuf& destBuf, unsigned cutVer = 0) const;
        TdrError::ErrorType pack(char* buffer, size_t size, size_t* usedSize = NULL, unsigned cutVer = 0) const;

        TdrError::ErrorType unpack(TdrReadBuf& srcBuf, unsigned cutVer = 0);
        TdrError::ErrorType unpack(const char* buffer, size_t size, size_t* usedSize = NULL, unsigned cutVer = 0);

        /* set indent = -1 to disable indent */
        TdrError::ErrorType visualize(TdrWriteBuf& destBuf, int indent = 0, char separator = '\n') const;
        TdrError::ErrorType visualize(char* buffer, size_t size, size_t* usedSize = NULL,
                                      int indent = 0, char separator = '\n') const;
        const char* visualize_ex(TdrWriteBuf& destBuf, int indent = 0, char separator = '\n') const;
        const char* visualize_ex(char* buffer, size_t size, size_t* usedSize = NULL,
                                 int indent = 0, char separator = '\n') const;


    public: /* version infomation */
        enum
        {
            BASEVERSION = 1,
            CURRVERSION = 1,
        };
};

typedef       CSReplaySvrNewBattleReq          tagCSReplaySvrNewBattleReq;
typedef       CSReplaySvrNewBattleReq             CSREPLAYSVRNEWBATTLEREQ;
typedef       CSReplaySvrNewBattleReq*          LPCSREPLAYSVRNEWBATTLEREQ;
typedef const CSReplaySvrNewBattleReq*         LCPCSREPLAYSVRNEWBATTLEREQ;


class CSReplaySvrPingReq
{
    public: /* members */
        uint8_t Reserved; // 保留字段

    public: /* methods */
        TdrError::ErrorType construct();

        TdrError::ErrorType pack(TdrWriteBuf& destBuf, unsigned cutVer = 0) const;
        TdrError::ErrorType pack(char* buffer, size_t size, size_t* usedSize = NULL, unsigned cutVer = 0) const;

        TdrError::ErrorType unpack(TdrReadBuf& srcBuf, unsigned cutVer = 0);
        TdrError::ErrorType unpack(const char* buffer, size_t size, size_t* usedSize = NULL, unsigned cutVer = 0);

        /* set indent = -1 to disable indent */
        TdrError::ErrorType visualize(TdrWriteBuf& destBuf, int indent = 0, char separator = '\n') const;
        TdrError::ErrorType visualize(char* buffer, size_t size, size_t* usedSize = NULL,
                                      int indent = 0, char separator = '\n') const;
        const char* visualize_ex(TdrWriteBuf& destBuf, int indent = 0, char separator = '\n') const;
        const char* visualize_ex(char* buffer, size_t size, size_t* usedSize = NULL,
                                 int indent = 0, char separator = '\n') const;


    public: /* version infomation */
        enum
        {
            BASEVERSION = 1,
            CURRVERSION = 1,
        };
};

typedef       CSReplaySvrPingReq          tagCSReplaySvrPingReq;
typedef       CSReplaySvrPingReq             CSREPLAYSVRPINGREQ;
typedef       CSReplaySvrPingReq*          LPCSREPLAYSVRPINGREQ;
typedef const CSReplaySvrPingReq*         LCPCSREPLAYSVRPINGREQ;


class CSReplaySvrFrameMd5Report
{
    public: /* members */
        int32_t BsRoomID; // 房间id
        int32_t frame; // 帧号
        int32_t length; // 长度
        char dumpInfo[MAX_DUMP_LEN]; // dump数据
        char protoMd5[MAX_DUMP_LEN]; // protoMd5数据

    public: /* methods */
        TdrError::ErrorType construct();

        TdrError::ErrorType pack(TdrWriteBuf& destBuf, unsigned cutVer = 0) const;
        TdrError::ErrorType pack(char* buffer, size_t size, size_t* usedSize = NULL, unsigned cutVer = 0) const;

        TdrError::ErrorType unpack(TdrReadBuf& srcBuf, unsigned cutVer = 0);
        TdrError::ErrorType unpack(const char* buffer, size_t size, size_t* usedSize = NULL, unsigned cutVer = 0);

        /* set indent = -1 to disable indent */
        TdrError::ErrorType visualize(TdrWriteBuf& destBuf, int indent = 0, char separator = '\n') const;
        TdrError::ErrorType visualize(char* buffer, size_t size, size_t* usedSize = NULL,
                                      int indent = 0, char separator = '\n') const;
        const char* visualize_ex(TdrWriteBuf& destBuf, int indent = 0, char separator = '\n') const;
        const char* visualize_ex(char* buffer, size_t size, size_t* usedSize = NULL,
                                 int indent = 0, char separator = '\n') const;


    public: /* version infomation */
        enum
        {
            BASEVERSION = 1,
            CURRVERSION = 1,
        };
};

typedef       CSReplaySvrFrameMd5Report          tagCSReplaySvrFrameMd5Report;
typedef       CSReplaySvrFrameMd5Report             CSREPLAYSVRFRAMEMD5REPORT;
typedef       CSReplaySvrFrameMd5Report*          LPCSREPLAYSVRFRAMEMD5REPORT;
typedef const CSReplaySvrFrameMd5Report*         LCPCSREPLAYSVRFRAMEMD5REPORT;


class CSReplaySvrGameEndReq // 结束比赛上报
{
    public: /* members */
        int32_t frame; // 帧号
        int32_t BsRoomID; // 房间id
        CSStatisticsBattle statisticsBattle; // 游戏统计数据
        int32_t dumpLength; // 长度
        char dumpInfo[MAX_DUMP_LEN]; // dump数据

    public: /* methods */
        TdrError::ErrorType construct();

        TdrError::ErrorType pack(TdrWriteBuf& destBuf, unsigned cutVer = 0) const;
        TdrError::ErrorType pack(char* buffer, size_t size, size_t* usedSize = NULL, unsigned cutVer = 0) const;

        TdrError::ErrorType unpack(TdrReadBuf& srcBuf, unsigned cutVer = 0);
        TdrError::ErrorType unpack(const char* buffer, size_t size, size_t* usedSize = NULL, unsigned cutVer = 0);

        /* set indent = -1 to disable indent */
        TdrError::ErrorType visualize(TdrWriteBuf& destBuf, int indent = 0, char separator = '\n') const;
        TdrError::ErrorType visualize(char* buffer, size_t size, size_t* usedSize = NULL,
                                      int indent = 0, char separator = '\n') const;
        const char* visualize_ex(TdrWriteBuf& destBuf, int indent = 0, char separator = '\n') const;
        const char* visualize_ex(char* buffer, size_t size, size_t* usedSize = NULL,
                                 int indent = 0, char separator = '\n') const;


    public: /* version infomation */
        enum
        {
            BASEVERSION = 1,
            CURRVERSION = 1,
        };
};

typedef       CSReplaySvrGameEndReq          tagCSReplaySvrGameEndReq;
typedef       CSReplaySvrGameEndReq             CSREPLAYSVRGAMEENDREQ;
typedef       CSReplaySvrGameEndReq*          LPCSREPLAYSVRGAMEENDREQ;
typedef const CSReplaySvrGameEndReq*         LCPCSREPLAYSVRGAMEENDREQ;


class CSSyncNewPlayer // 新玩家加入战斗
{
    public: /* members */
        int32_t PlayerID; // 玩家ID
        BattleCreateParams CreateParams; // 战斗创建数据
        CSPoint CurPos; // 玩家位置
        CSPoint CurDir; // 玩家朝向

    public: /* methods */
        TdrError::ErrorType construct();

        TdrError::ErrorType pack(TdrWriteBuf& destBuf, unsigned cutVer = 0) const;
        TdrError::ErrorType pack(char* buffer, size_t size, size_t* usedSize = NULL, unsigned cutVer = 0) const;

        TdrError::ErrorType unpack(TdrReadBuf& srcBuf, unsigned cutVer = 0);
        TdrError::ErrorType unpack(const char* buffer, size_t size, size_t* usedSize = NULL, unsigned cutVer = 0);

        /* set indent = -1 to disable indent */
        TdrError::ErrorType visualize(TdrWriteBuf& destBuf, int indent = 0, char separator = '\n') const;
        TdrError::ErrorType visualize(char* buffer, size_t size, size_t* usedSize = NULL,
                                      int indent = 0, char separator = '\n') const;
        const char* visualize_ex(TdrWriteBuf& destBuf, int indent = 0, char separator = '\n') const;
        const char* visualize_ex(char* buffer, size_t size, size_t* usedSize = NULL,
                                 int indent = 0, char separator = '\n') const;


    public: /* version infomation */
        enum
        {
            BASEVERSION = 1,
            CURRVERSION = 1,
        };
};

typedef       CSSyncNewPlayer          tagCSSyncNewPlayer;
typedef       CSSyncNewPlayer             CSSYNCNEWPLAYER;
typedef       CSSyncNewPlayer*          LPCSSYNCNEWPLAYER;
typedef const CSSyncNewPlayer*         LCPCSSYNCNEWPLAYER;


#pragma pack()

}
#endif
